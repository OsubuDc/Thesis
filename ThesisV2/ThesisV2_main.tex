\documentclass[11pt,a4paper,twoside,openright]{book}

% ===== LANGUAGE AND ENCODING =====
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

% ===== FONTS =====
\usepackage{lmodern}
\usepackage{microtype}

% ===== MATH =====
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{bm}
\usepackage{siunitx}

% ===== FIGURES =====
\usepackage{graphicx}
\usepackage{float}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}
\graphicspath{{Graphics/}}   % put all images in a figures/ subfolder
\usepackage{svg}
% ===== TABLES =====
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}

% ===== CODE LISTINGS =====
\usepackage{listings}
\usepackage{xcolor}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.97,0.97,0.97}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{blue}\bfseries,
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=8pt,
    frame=single,
    framerule=0.5pt,
    rulecolor=\color{black!30}
}
\lstset{style=mystyle}

% ===== BIBLIOGRAPHY =====
\usepackage{csquotes}
\usepackage[
  backend=biber,
  style=ieee,
  sorting=none,
  maxbibnames=99
]{biblatex}
\addbibresource{references.bib}  % <-- update path if needed

% ===== PAGE GEOMETRY =====
\usepackage[
  inner=3cm,
  outer=2.5cm,
  top=2.5cm,
  bottom=3cm,
  headheight=14pt
]{geometry}

% ===== HEADERS AND FOOTERS =====
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\nouppercase{\leftmark}}
\fancyhead[RO]{\nouppercase{\rightmark}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ===== CHAPTER STYLING =====
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titlespacing*{\chapter}{0pt}{0pt}{40pt}

% ===== LINE SPACING =====
\usepackage{setspace}
\onehalfspacing

\setlength{\parindent}{0pt}

% ===== TABLE OF CONTENTS STYLING =====
\usepackage{tocloft}
\renewcommand{\cftchapfont}{\bfseries}
\renewcommand{\cftchappagefont}{\bfseries}

% ===== HYPERLINKS =====
\usepackage[
  colorlinks=true,
  linkcolor=black,
  citecolor=blue,
  urlcolor=blue,
  bookmarks=true,
  bookmarksnumbered=true,
  pdfstartview=FitH,
  pdftitle={Thesis_OskarDeClerck_2026},   % <-- update
  pdfauthor={Oskar De Clerck}
]{hyperref}

% ===== CUSTOM COMMANDS =====
% Bold paragraph heading that does NOT appear in the ToC
\newcommand{\smalltitle}[1]{%
  \vspace{0.5cm}%
  \noindent\textbf{#1}%
  \vspace{0.2cm}%
  \par\noindent%
}

% To erase space between list items.
\usepackage{enumitem}

% ===== DOCUMENT METADATA =====
\title{Robotic Testbench for Collaborative Handling of Payloads with Varying Weights using Compliant Joint Transmissions}
\author{Oskar De Clerck}
\date{May 2026}


% ============================================================
\begin{document}
% ============================================================

% ===== TITLE PAGE =====
\begin{titlepage}
    \centering
    \vspace*{2cm}

    {\Large\scshape KU Leuven\par}
    \vspace{0.5cm}
    {\large Faculty of Engineering Technology\par}
    \vspace{2.5cm}

    {\Huge\bfseries Implementation of a novel compliant transmission in a 3 DoF robotic testbench for collaborative hand guiding tasks\par}   % <-- update
    \vspace{0.5cm}
    \rule{10cm}{0.4pt}\par
    \vspace{3cm}

    {\Large\itshape Oskar De Clerck\par}
    \vfill

    {\large A thesis submitted for the degree of\\
    Master of Science in \textit{industriële wetenschappen: Elektromechanica}\par}
    \vfill

    {\large Academic Year 2025--2026\par}
    \vspace{0.5cm}
    {\large Promotor: ir.\ Maurizio Grossi\par}
    {\large Copromotor: prof.\ dr.\ ir.\ Dino Accoto\par}
\end{titlepage}


% ============================================================
%  FRONT MATTER
% ============================================================
\frontmatter

\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

Musculoskeletal disorders caused by non-ergonomic load handling remain one of the leading occupational health concerns in the packaging and assembly industry. Passive weight-compensating devices are widely used but cannot adapt to varying weights, while collaborative robots typically require software interaction for payload calibration and restrict compliant interaction to a sensorized end-effector.

This master's thesis presents a three degree of freedom robotic testbench using transmissions with an integrated spring system. These transmissions combine intrinsically safe interaction with weight measurement and intuitive hand guidance at any point along the arm structure, without requiring prior software configuration by the operator.

The spring element enables continuous torque measurement at the joints of the robotic arm without the use of dedicated force or torque sensors. A C++ controller implements a state machine with calibration, weighing, carrying and put-down modes. Gravity compensation is computed via recursive Newto--Euler inverse dynamics, while a trim integrator corrects for unmodelled effects during the calibration phase. Payload weight is identified from steady-state spring deflection when the arm lifts an unknown load, without software interaction from the operator.

The system is built around two Maxon brushless DC motors with ESCON2 motor drivers in CANopen current--command mode, an NVIDIA Jetson Orin NX as the main controller, and an STM32G4 microcontroller as a CAN sensor bridge for magnetic position sensors and spring deflection measurements.

The mechanical design combines weight-optimised sheet metal parts in aluminium and stainless steel with weight--optimised milled aluminium components. Additionally, attention was given to tolerance chains for the alignment of the transmission shafts, couplings and joint axes.

The testbench provides a dedicated platform for validating the compliant transmission concept in interactive load--handling scenarios and for quantifying the ergonomic benefit of the proposed approach in industrial assembly and packaging contexts. 

\cleardoublepage

\chapter*{Acknowledgements}
\addcontentsline{toc}{chapter}{Acknowledgements}
\cleardoublepage

\tableofcontents
\cleardoublepage

\listoffigures
\cleardoublepage

\listoftables
\cleardoublepage

\chapter*{Nomenclature}
\addcontentsline{toc}{chapter}{Nomenclature}

\section*{Symbols}
\begin{tabular}{@{}lll@{}}
  $\bm{q}$    & Joint position vector  & [rad]    \\
  $K_s$       & Spring stiffness       & [Nm/rad] \\
  % add rows here
\end{tabular}

\vspace{1cm}
\section*{Abbreviations}
\begin{tabular}{@{}ll@{}}
  CAN  & Controller Area Network \\
  SET  & Series Elastic Transmission \\
  % add rows here
\end{tabular}

\cleardoublepage


% ============================================================
%  MAIN MATTER
% ============================================================
\mainmatter

% ============================================================
\chapter{Introduction}
\label{ch:introduction}
% ============================================================

\section{Background and Motivation}

\subsection{Musculoskeletal disorders and ergonomic work environments}

Non-ergonomic work environments place strain on the human body, which can cause painful complications and limit the worker’s ability to perform tasks optimally. Ergonomics can be defined as the study of work, where the goal is to fit tasks and jobs to workers’ capabilities, rather than forcing workers to adapt to the job. An ergonomic work environment therefore implements task-specific strategies to prevent reduced work ability. How these complications arise, which parts of the body are affected, and how they can be prevented are explained in this section. 

The musculoskeletal system contains muscles, bones, joints and connective tissues like tendons and ligaments. Musculoskeletal disorders (MSDs) can therefore be defined as injuries of the muscles, nerves, tendons, joints, cartilage, and spinal discs. When these injuries are sustained or worsened by exposure to risk factors in the workplace, they are also known as work-related musculoskeletal disorders (WMSDs). These disorders can go from minor aches to severe pain and chronic illness requiring medical treatment, potentially preventing workers from performing physical tasks. 

WMSDs develop over time due to non-ergonomic working conditions and can be categorized into two main groups:
\vspace{0.2cm} 
\begin{itemize}[noitemsep, topsep=0pt]
	\item Back disorders related to handling loads
	\item Upper limb disorders, also known as repetitive strain injuries (RSIs)
\end{itemize}
\vspace{0.3cm}
MSDs can be caused by, for example:
\vspace{0.2cm}
\begin{itemize}[noitemsep, topsep=0pt]
    \item Twisting and bending movements while handling loads
    \item Repetitive or forceful movements 
    \item Awkward and static postures
    \item Exposure to vibrating tools
    \item Prolonged sitting or standing in the same position
\end{itemize}
\vspace{0.3cm}

\newpage

An increase in MSDs has been reported worldwide. This rise is partly attributed to improved recognition and reporting, but also to the tendency for work to be more segmented and specialized. This trend is driven by an increasingly competitive global economy, where fast-paced repetitive work environments are more widespread.

\subsection{Industrial motivation for ergonomic improvement}

Musculoskeletal disorders affect approximately one in three people worldwide. Within the European Union, MSDs represent the most costly health-related problems, affecting 45 million workers. Many individuals are unable to continue working beyond the age of 60 due to these conditions, resulting in billions of euros in lost revenue and a substantial burden on the public healthcare system.

A declining physical condition can also lead to mental health issues such as depression or frustration, resulting in reduced job satisfaction and, indirectly, lower work performance, even when workers are still physically able to continue working.

The increase in education in the workforce, combined with higher expectations regarding job satisfaction, has reduced workers' tolerance for WMSDs. An ergonomic work environment can prevent the development of WMSDs and directly reduce both the cost to companies due to sick leave and the societal impact.  

Reduced work ability can be categorized into absenteeism and presenteeism. Absenteeism refers to the absence from work due to injuries sustained on the job. Presenteeism describes the situation in which workers continue to attend their expected work duties despite existing injuries, often due to concerns about job security. In such cases, work is performed
in a self--protective manner to reduce physical strain.

In Belgium, 2.5 million people were affected by MSDs in 2018 (approximately 62\% of the Belgian working population in 2015), resulting in 3 billion euros in direct medical costs and 2 billion in work absenteeism, corresponding to 0.3\% of Belgium's gross domestic product
\cite{oralEvaluationAbsenteeismPresenteeism2024}\cite{yassiRepetitiveStrainInjuries}\cite{vilellaMusculoskeletalExamination}\cite{ibvMusculoskeletalDisordersCaused2017}\cite{gorassoHealthEconomicBurden2023}.

\subsection{Physical human--robot interaction}
\label{subs:ph-h-r}
There is a clear need to address reduced work efficiency resulting from physical injuries since this improves the quality of life of an increasingly older workforce, while also reducing the impact on social welfare systems and increasing overall economic productivity. Industry has already adopted robotic solutions to achieve accuracy, speed, and repeatability that human workers cannot provide. Humans, however, provide flexibility and have the capability to react to unforeseen circumstances. By increasing the degree of human participation, the strength and repeatability of robots are combined with the adaptability and decision--making skills of humans. Humans and robots each operate in their respective workspaces to perform their tasks, and in some cases share the same workspace, as illustrated in Figure~\ref{fig:workspaces}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{workspaces}
    \caption{Human, robot, and shared workspace from Fraunhofer
             \cite{benderLightweightRobotsManual2016}.}
    \label{fig:workspaces}
\end{figure}

Human--robot cooperation generally refers to the use of robots without safety fencing, also called cage--free robots. This is in contrast to traditional robots which are stiff, heavy, and lack safety features that allow them to operate in the same space as humans, and are consequently only allowed to operate in closed --off workspaces.

Humans and robots can work together in different types of operation, as illustrated in Figure~\ref{fig:hr-cooperation-levels}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{Human_robot_collaboration}
    \caption{Levels of cooperation between human worker and robot from Fraunhofer
             \cite{benderLightweightRobotsManual2016}.}
    \label{fig:hr-cooperation-levels}
\end{figure}

From left to right an increasing level of interaction is shown.

\vspace{0.2cm} 
\begin{itemize}[noitemsep, topsep=0pt]
	\item Cell: Robot operates in traditional cage, fenced--off from human contact
	\item Coexistence: Human and cage--free robot work at the same time, but in different workspaces
	\item Synchronized: Humans and cage--free robots work in the same workspace, but one at a time
	\item Cooperation: Human and cage--free robot have tasks in the same workspace at the same time, but do not work together on the same task
	\item Collaboration: Human and cage--free robot work in the same workspace on the same task
\end{itemize}
\vspace{0.3cm}

The robotic testbench developed in this thesis operates in collaboration mode during assisted load handling, when the human operator physically guides the robotic arm, and in coexistence mode during the floating stand--by state, when no physical interaction is taking place. This distinction is further clarified in chapter (idk exactly for now).

Collaborative robots, defined as robots capable of performing collaborative tasks as described above, are widely used in industry. However, they are still primarily applied in traditional robotic operations such as pick--and--place tasks. In many cases, humans and robots remain either physically separated or operate in a coexistence mode, where no physical barrier is present but no direct interaction takes place. Such implementations are only economically viable for large batch sizes, since a whole production line needs to be tuned towards that specific robotic solution. 

Cage--free robotic arms that share a workspace with a human worker are introduced to increase production flexibility. Interactive robotic solutions for small batches and varying product sizes have become a clear need since 70\% of collaboration between human workers and robots occurs in assembly applications. An industry survey reported that 86\% of respondents were willing to invest in robotic solutions, indicating strong industrial interest. The primary motivations for introducing robotic arms in assembly environments are:

\vspace{0.2cm} 
\begin{itemize}[noitemsep, topsep=0pt]
	\item Improving operational efficiency
	\item Improving ergonomics
\end{itemize}
\vspace{0.3cm}

Cage--free robotic solutions generally involve higher initial costs compared to traditional fenced systems due to safety certification requirements and conformity procedures to minimize the risk of injuries. However, companies are often willing to accept longer payback periods for implementing and testing company--specific robotic solutions which increase ergonomics \cite{benderLightweightRobotsManual2016}\cite{europeanparliament.directorategeneralforparliamentaryresearchservices.AnalysisExploringRisks2023}.

ISO/TS 15066 covers the safety requirements for collaborative operations between human workers and robots. This normative classifies four different collaborative operations modes \cite{rosenstrauchHumanRobotCollaboration2018}\cite{chemwenoOrientingSafetyAssurance2020}: 

\vspace{0.2cm} 
\begin{itemize}[noitemsep, topsep=0pt]
	\item Safety--rated monitored stop
	\item Hand guiding
	\item Power and force limiting
	\item Speed and separation monitoring
\end{itemize}
\vspace{0.3cm}

\smalltitle{Safety--rated monitored stop}
The robot operates at its rated speed when a human is not present in the collaborative workspace. When a human enters the workspace, which can be detected by various means like laser scanners or light curtains, the robot undergoes a complete stop. As long as the human is in the workspace, the robot will not operate. Only by leaving the workspace and resetting the safety stop, can operation continue.

\smalltitle{Hand guiding}
Hand guided robotic applications are defined as a direct physical interaction between humans and robots, where the human operator has full control over the robot's motion. In this approach, a direct input device is located at or near the end--effector to measure input forces and simultaneously define the position of the human in the shared workspace. The robot must be in a monitored stop before hand guiding can be activated via an enabling device. Safety requirements for this mode include force and/or torque sensing at the guiding point, emergency stop accessible from the position where the robot is being guided, which is typically at the end--effector, and a maximum speed limit during guided motion.

\smalltitle{Power and force limiting}
This mode is the most commonly associated with collaborative robots. The robot's software and hardware are designed in such a way that any contact with a human cannot result in injury. Low robot mass, rounded surfaces, and padding are combined with active force and torque sensing, which limits contact forces with the environment below defined thresholds depending on the body region of the human that can be in contact with the robot.

\smalltitle{Speed and separation monitoring}
Robots and humans can operate in the same workspace at the same time, but the position and speed of the human and robot relative to each other is constantly monitored to adjust the speed of the robot with the goal of constantly having a minimum dynamical separation distance at all times. \\

Hand guiding is useful for packaging and assembly applications since it combines the physical strength of robotic arms, which can compensate for the weight of components or help with move relatively light but large components, with human intelligence.

ISO/TS 15066 specifies that the hand--guiding devices should be located at or near the robot end--effector, structurally restricting compliant interaction to a single designated contact point. Collisions or attempted handling of the robotic arm elsewhere will result in a safety stop \cite{sicilianoSpringerHandbookRobotics2016a}.

Existing robotic solutions that allow a human worker to hand guide a robotic arm while simultaneously identifying and compensating for payloads of unknown variable weights, without requiring prior software configuration, are to the best of the author's knowledge, not available in assembly and packaging industries. When hand guiding is discussed in literature, it is typically limited to sensorized end--effectors on conventional stiff robots. Collaborative robots adopted in industry generally lack build--in compliance. As previously stated, collaborative robots are used for applications with little to no human contact, where precision and repeatability is more important than inherent compliance \cite{gregorHandGuidingVirtual2021}\cite{hameedControlSystemDesign2023}.

This absence in current industrial applications is further supported by surveys of end users, system integrators and researchers, which indicate that collaborative robots are still primarily used as conventional stiff robots with limited or no direct human interaction, while offering the added value of less space requirements for operation \cite{montiniCollaborativeRoboticsSurvey2024}.

Current collaborative robots with the highest payloads, such as the UR30 from Universal Robots and the CR-35iB from FANUC, can handle loads up to 30kg and 50kg respectively. Still, hand guiding is mainly used for point-- and path--based teaching and not for weight compensation tasks in hand guiding applications. The user can intuitively move the robot to a new point in space and record that position. This can be done for a single point or for a full path; in this case the robot records the continuous movement while a button is pressed. It is, however, possible to correctly configure collaborative robots for weight compensation, but software is required for correct calibration of the load attached to the end--effector. Although rare, some collaborative robots can estimate payload weight and center of mass by performing predefined controlled motions, but multiple interactions with the software controller are needed, making this approach time--consuming. This method is not generally available, and is often part of an optional software package that is not compatible with every collaborative robot. This confirms that automatic real--time payload identification without software interaction is not a standard feature of current collaborative platforms deployed in assembly and packaging contexts industries, representing a key limitation addressed by this thesis \cite{UR30CollaborativeRobot}\cite{CollaborativeRobotsCobots}\cite{groupHandGuidingRobots2021}\cite{ConfiguringFANUCRobot2026}. 

\subsection{Weight assisting devices}
\label{subsec:weight_assisting_devices}
Passive weight compensating manipulators are well established in industrial environments to help human workers perform their tasks in a more ergonomically sustainable way. These devices, if well calibrated, allow payloads to be moved around with minimal effort, offering passive, purely mechanically sourced weight compensation, effectively combining software--free and intuitive use. These manipulators however share a fundamental limitation; they only operate efficiently only within a pre-tuned weight range. The further a payload differs from the equilibrium point, the more force the human operator will have to deliver to move the load fixed to the manipulator. As a direct consequence, varying payloads that are too different in weight cannot be manipulated with these devices without manually re--tuning the counter--force system, for example counterweights and gas springs \cite{arakelianGRAVITYCOMPENSATIONROBOTICS2019}. The proposed system replicates the intuitive, software--free interaction properties of a passive balancer, while adding real--time load identification and the possibility to perform autonomous tasks, integrating this solution into the production line. Some commercially available examples support this claim. 

\newpage

The company 3ARM produces commercially available weight compensating manipulators, an example of which shown in Figure ~\ref{fig:3arm_manipulator}. Pneumatic gas springs are used to compensate weights within a minimum and maximum payload limit. The gas springs can be manually tuned to compensate for different weight classes \cite{IndustrialLiftAssist}. Figure ~\ref{fig:Gas-spring-characteristic} presents an example of a gas spring characteristic. By adjusting the filling pressure, the spring can optimized for different payloads scenarios \cite{CataloguesUserManuals}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{3arm}
    \caption{3ARM manipulator
             \cite{IndustrialLiftAssist}.}
    \label{fig:3arm_manipulator}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Gasspringcharacteristic}
    \caption{Gas spring characteristic by Suspa \cite{CataloguesUserManuals}.}
    \label{fig:Gas-spring-characteristic}
\end{figure}

\newpage

Transprotec offers spring balancers, showh in Figure ~\ref{fig:spring-balancer}, that are tuned to a specific weight, load changes are not permitted to guarantee safety and correct operation \cite{SpringBalancerBalancer}. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.1\textwidth]{Springbalancer}
    \caption{Spring balancer from Transprotec \cite{SpringBalancerBalancer}.}
    \label{fig:spring-balancer}
\end{figure}

\subsection{Intelligent Assist Devices}

The robotic testbench proposed in this thesis belongs to the category of Intelligent Assist Devices (IADs). These are described as computer--controlled, servo--driven tools that enable human workers to manipulate payloads in a safe and ergonomic way. This differentiates IADs from the devices described in Section~\ref{subsec:weight_assisting_devices}, which were completely passive. IADs were introduced to increase productivity and mitigate non--ergonomic work environments, a major issue in societies with an aging workforce. Currently, the term IADs is not commonly used anymore, as it has been subsumed under the modern term collaborative robots \cite{Cobot2026}. When IADs are mentioned today, they primarily appear as overhead gantry--mounted installations. While IADs can be effective in dedicated production cells with sufficient overhead clearance, they are unsuitable for industrial environments where ceiling height is limited, or overhead obstructions are present. 

IADs cannot only interact safely and compliantly with human operators, but can also actively follow a predefined path. An example of this semi--autonomous strategy can be found in the automotive industry, where the controller guides the payload along a moving assembly line so that the human operator can focus on correctly installing the part into the assembly, rather than continuously repositioning the payload, as shown in Figure~\ref{fig:spring-balancer}\cite{IndustrialManipulatorsLoad}\cite{colgateIntelligentAssistDevices2003}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.4\textwidth]{assemblyline}
    \caption{IAD integrated in an assembly line \cite{colgateIntelligentAssistDevices2003}.}
    \label{fig:spring-balancer}
\end{figure}

\newpage

\section{Problem Statement}

Non--ergonomic load handling is the primary cause of musculoskeletal disorders in packaging and assembly industries. Passive weight compensating devices are well established in industry with the goal of reducing this physical strain on the human body; these devices however cannot adapt to varying payloads and provide no sensing, intelligence, or active integration with production lines.

Intelligent assist devices offer semi--autonomous advantages to support payload manipulation. Gantry--style implementation cannot be used in buildings with limited ceiling heights or overhead obstructions. IADs are semi--autonomous solutions and can thus offer a higher degree of integration than passive devices.

Collaborative robots require frequent software interaction to calibrate the system for different payloads or work modes. Although rare, it is possible for specific collaborative robots to go through predefined motions to estimate the weight of a payload rigidly attached to the end--effector, which also requires software programming and confirmation. Payloads also need to be fixed to the end--effector, which is not always possible for loads that vary in shape and size.

This thesis presents a three degree of freedom robotic testbench using novel, inherently compliant transmissions. This allows for the combination of an intrinsically safe design with real--time weight measurements and intuitive, software--free hand guidance at any point along the arm structure. This approach aims to combines the strengths of collaborative robots with passive or intelligent assist devices.

The testbench provides a weight--optimized platform that allows for further validation of the novel transmission, offering data acquisition and a configurable controller written in C++ specifically for the approach of this thesis. 
\section{Thesis Outline}

In Chapter~\ref{ch:literature}, the theoretical background on concepts relevant to this thesis is introduced, followed by an overview of the current state of the art in collaborative load--handling systems.\\

In Chapter~\ref{ch:methodology}, the design of the robotic testbench is presented; this includes the mechanical design, hardware implementation, proposed controller, and data communication architecture.\\

In Chapter~\ref{ch:validation}, the developed testbench and proposed controller are experimentally validated; this includes mechanical design validation and controller performance evaluation in interactive load--handling experiments.\\

Chapter~\ref{ch:conclusion} summarizes the main conclusions  of this thesis, discusses limitations of the proposed system, and presents possible improvements for future development.
% ============================================================
\chapter{Literature Review}
\label{ch:literature}
% ============================================================

\section{Theoretical Background}

\subsection{Impedance control}

This section provides a theoretical background on impedance and admittance control, which are widely adopted frameworks for physical human--robot interaction. Although these strategies were studied in depth to inform the design of the control architecture, full implementation was not pursued at this stage. The mechanical impedance parameters of the novel transmissions are unknown prior to experimental characterisation, making a reliable impedance controller difficult to tune and validate. Once the physical behaviour of the transmissions has been identified through testing, impedance control represents a natural extension of the current work.

Impedance control is widely used in applications that involve physical human--robot interaction. The interaction forces between the robotic structure and its environment can be regulated by adjusting the apparent mechanical impedance of the robot. Mechanical impedance is defined as the ratio of the output force to the input velocity. It represents how much the robotic structure resists motion when an external force is applied.

\begin{equation}
    Z(s) = \frac{F(s)}{v(s)}
    \label{eq:impedance_definition}
\end{equation}

Its inverse, mechanical admittance is defined as the ratio of input velocity to the output force.

\begin{equation}
    Y(s) = \frac{v(s)}{F(s)} = \frac{1}{Z(s)}
    \label{eq:admittance_definition}
\end{equation}

When two mechanical bodies interact, the interaction can be characterized by their relative mechanical properties. A body that resists motion by responding to displacement with forces is modelled as a mechanical impedance. A body that responds to forces by generating motion or deformation is modelled as a mechanical admittance. These roles are not inherent properties of the objects themselves, but a consequence of the mechanical properties of the interacting bodies relative to each other, and the choice of input and output signals. 

In conventional robotic applications, the robotic structure is stiff, position--controlled, and has a high inertia compared to its environment, like a human worker or payloads. It follows a defined trajectory and resists external forces; in this scenario, the robotic structure behaves as an impedance. The environment, whether it is a workpiece, or a human worker, yields to externally applied forces and is typically modelled as an admittance. As an example, pushing a trolley or compressing a pillow by applying an external force can clarify this concept; in both cases the environment moves or deforms in response to an applied force. 

The impedance or admittance assignment is context--dependent. A rigid concrete wall presents an impedance that is much higher than that of a lightweight robotic structure; from the perspective of the robotic structure, it must yield to its much stiffer environment. 

A human operator physically guiding a robotic arm applies forces and expects the robotic structure to move; the human worker behaves like an impedance, while the robotic structure behaves like an admittance. Impedance control modifies the mechanical impedance of a robotic structure to control how it behaves relative to its environment. It achieves this by shaping the relationship between the motion of the robot and the forces it exchanges with its environment, so that from the outside, the robot feels like a different mechanical system than it physically is.

Figure~\ref{fig:impedance_control} illustrates a robotic manipulator in contact with its environment. The block labelled 'desired impedance' at the end--effector represents the target dynamic behaviour that the controller imposes at the contact point .

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{impedance_control}
    \caption{Impedance control for robotic structure in contact with an environment \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:impedance_control}
\end{figure}

Impedance control mirrors the ability of the human body to change its mechanical impedance by tensioning its muscles. To guarantee safe interaction between a robotic structure and its environment, one of the two bodies should be compliant relative to the other. Most collaborative robots lack the ability to dynamically change their mechanical impedance; therefore, safe interaction between between a robot and an unknown environment cannot be guaranteed by mechanical design alone. Impedance control allows for a user--defined, dynamic relationship between the trajectory of the end--effector and the contact forces from the environment, allowing stiff robotic structures to compliantly react to their environment.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{impedance_control_u}
    \caption{Mass--spring--damper system in contact with external force $f_e$ \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:impedance_control_msd}
\end{figure}

The mathematical derivation for impedance control starts by considering a second--order system as shown in Figure~\ref{fig:impedance_control_msd}.
\begin{equation}
    m\,\ddot{x} + b\,\dot{x} + k\,x = u + f_{e}
    \label{eq:plant_dynamics}
\end{equation}

In this equation, $m$ represents the system mass, $b$ the damping coefficient, $k$ the stiffness coefficient, $u$ the control input (as seen from the plant's perspective) generated by the controller which is applied to the plant dynamics, and $f_e$ is the external force applied to the system. Here, $x$ denotes the absolute position of the system mass, which equals $x_e$ at the contact surface. The reference position $x_r$ defines the equilibrium point the system would occupy in the absence of contact. It is the displacement from this equilibrium, $(x - x_r)$, that appears in the target impedance equation~\ref{eq:target_impedance} and drives the desired dynamic response.

The idea of impedance control is to change the apparent impedance of a mechanical  system. By defining a target impedance, the mechanical impedance of the system can be modified. This target impedance is expressed as a virtual mass--spring--damper system:
\begin{equation}
    m_{d}\,(\ddot{x}-\ddot{x}_{r})
    + b_{d}\,(\dot{x}-\dot{x}_{r})
    + k_{d}\,(x-x_{r})
    = f_{e},
    \label{eq:target_impedance}
\end{equation}

In this equation, $m_d$, $b_d$, and $k_d$ respectively represent the desired inertia, damping, and stiffness of the system. A lower $k_d$ makes the robotic structure more compliant, while a higher $k_d$ makes it stiffer. The damping $b_d$ opposes velocity and influences how fast the system will settle. The inertia $m_d$ determines the system's resistance to acceleration.

A general classification of impedance control approaches is shown in Figure~\ref{fig:impedance_control_appr}. The conventional approaches are discussed in this thesis, as these provide a sufficient conceptual understanding of the topic \cite{al-shukaActiveImpedanceControl2018, songImpedanceControlRobots2017, andersonHybridImpedanceControl1988, songTutorialSurveyComparison2019}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{generalapproachimpedancecontrol}
    \caption{General classification of the impedance control approaches \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:impedance_control_appr}
\end{figure}

\subsubsection{Force--based impedance control}
In force--based impedance control, the control input $u$ is a force or torque generated by the controller and applied to the plant. Consequently, the motor drivers must be capable of directly controlling their output torque; drivers limited to position or velocity control cannot directly implement an impedance controller. For precisely this reason, in this thesis the motor drivers are operated in current--command mode, as described in Section~\ref{}. 

The control law is derived by requiring that the system satisfies the target impedance of Equation~\eqref{eq:target_impedance}. In the first step, the plant dynamics~\eqref{eq:plant_dynamics} are solved for the system's acceleration term $\ddot{x}$:
\begin{equation}
    \ddot{x} = \frac{1}{m}\bigl(u + f_{e} - b\,\dot{x} - k\,x\bigr).
    \label{eq:plant_accel}
\end{equation}

The target impedance~\eqref{eq:target_impedance} is solved for the desired acceleration $\ddot{x}$:
\begin{equation}
    \ddot{x} = \ddot{x}_{r} + m_{d}^{-1}\bigl(f_{e} - b_{d}\,(\dot{x}-\dot{x}_{r}) - k_{d}\,(x-x_{r})\bigr).
    \label{eq:desired_accel}
\end{equation}

For the system to satisfy the desired impedance behaviour, the actual acceleration~\eqref{eq:plant_accel} and the desired acceleration~\eqref{eq:desired_accel} must equal each other.
\begin{equation}
    \frac{1}{m}\bigl(u + f_{e} - b\,\dot{x} - k\,x\bigr)
    = \ddot{x}_{r} + m_{d}^{-1}\bigl(f_{e} - b_{d}\,(\dot{x}-\dot{x}_{r}) - k_{d}\,(x-x_{r})\bigr).
    \label{eq:accel_equality}
\end{equation}

Multiplying both sides by $m$ and solving for $u$:
\begin{equation}
    u = b\,\dot{x} + k\,x - f_{e}
      + m\,\ddot{x}_{r}
      + m\,m_{d}^{-1}\bigl(f_{e} - b_{d}\,\dot{x} + b_{d}\,\dot{x}_{r} - k_{d}\,x + k_{d}\,x_{r}\bigr).
    \label{eq:impedance_expanded}
\end{equation}

Collecting terms in $\dot{x}$, $x$, and $f_{e}$ gives the force--based impedance control law as presented in Equation~\eqref{eq:impedance_general}. 
\begin{equation}
    u = \bigl(b - m\,m_{d}^{-1}\,b_{d}\bigr)\,\dot{x}
      + \bigl(k - m\,m_{d}^{-1}\,k_{d}\bigr)\,x
      - \bigl(1 - m\,m_{d}^{-1}\bigr)\,f_{e}
      + m\,m_{d}^{-1}\,\bigl(b_{d}\,\dot{x}_{r} + k_{d}\,x_{r} + m_{d}\,\ddot{x}_{r}\bigr).
    \label{eq:impedance_general}
\end{equation}

When desired inertia $m_d$ equals the actual inertia $m$, the inertia ratio $m\,m_{d}^{-1}$ becomes unity. This results in the external force term $f_e$ being canceled, and Equation~\eqref{eq:impedance_general} simplifies to:
\begin{equation}
    u = (b - b_{d})\,\dot{x}
      + (k - k_{d})\,x
      + b_{d}\,\dot{x}_{r}
      + k_{d}\,x_{r}
      + m\,\ddot{x}_{r}.
    \label{eq:impedance_md_equal_m}
\end{equation}

The elimination of $f_e$ from the control law holds only when $m = m_d$ exactly. In practice, the effective inertia of a robotic structure cannot be perfectly matched by a fixed nominal value, resulting in behaviour that does not fully conform to the simplified control law. Since the term representing the external force has been canceled, no force sensor is required. This formulation represents a velocity and position feedback with feed--forward desired trajectory values. 

An important limitation of this approach is that contact force is only controlled indirectly by specifying a reference position. For example, for a rigid robotic structure in free space, the stiffness $k$ is considered \SI{0}{\newton\per\metre} and the desired spring constant $k_d$ is set to \SI{500}{\newton\per\metre}. If a contact force of \SI{10}{\newton} is desired, the deviation from the reference position must be exactly \SI{20}{\milli\metre}. This cannot be guaranteed unless the physical properties and position of the environment are exactly known, which is not always feasible. As a consequence, impedance control can either guarantee accurate position tracking or force tracking. A stiff value for $k_d$ will produce high forces for small position errors resulting in accurate position tracking. A soft spring will result in accurate force control, but with large position errors as a trade--off.
A diagram of impedance control is shown in Figure~\ref{fig:impedance_control_diagram}. Note that only a reference position is used; whether reference velocity and acceleration are also considered depends on the application and the available measurements \cite{al-shukaActiveImpedanceControl2018, songImpedanceControlRobots2017, andersonHybridImpedanceControl1988, songTutorialSurveyComparison2019}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{forcebaseddiagram}
    \caption{Impedance control diagram, with reference position $x_r$, external force $f_e$ which in theory can be canceled, and a measured position $x$ \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:impedance_control_diagram}
\end{figure}

\subsubsection{Position--based impedance control}
Position--based impedance control is commonly referred to as admittance control, and is, as previously stated, the inverse of impedance control which was discussed above. Where impedance control reacts to motion deviation by generating forces, admittance control reacts to forces by deviating from the desired motion. Contrary to impedance control, a force measurement is required, meaning that sensors need to be fitted to the contact point with the environment.

A position--based impedance controller consists of two control loops as shown in Figure~\ref{fig:admittance_control_diagram}. The outer loop imposes the desired target impedance by generating a commanded reference position $x_c$, the inner position loop tracks that compliant reference position generated by the outer loop. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{admittancecontrolscheme}
    \caption{Admittance control diagram, with reference position $x_r$, compliant reference position $x_c$, measured position $x$, and measured external force $f_e$ \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:admittance_control_diagram}
\end{figure}

The outer loop requires both the reference position $x_r$ and the measured external force $f_e$ as input to compute the value for $x_c$ as shown in Equation~\eqref{eq:admittance_filter}, where $x_c$ is the only unknown variable.
\begin{equation}
    m_{d}\,(\ddot{x}_{c}-\ddot{x}_{r})
    + b_{d}\,(\dot{x}_{c}-\dot{x}_{r})
    + k_{d}\,(x_{c}-x_{r})
    = f_{e}.
    \label{eq:admittance_filter}
\end{equation}

When no external force is measured ($f_e = \SI{0}{\newton}$), the commanded impedance reference position $x_c$ must equal $x_r$. When the external force $f_e \neq 0$, $x_c$ deviates from $x_r$ according to the desired impedance parameters $m_d$, $k_d$, and $b_d$. For example, when an external force $f_e = \SI{10}{\newton}$ is applied and $k_d = \SI{500}{\newton\per\metre}$, ignoring the velocity and acceleration terms at steady--state, a deviation of $x_c - x_r = \SI{20}{\milli\metre}$ is calculated. 

This commanded position is used as the input for the inner loop, which can be a conventional position controller such as a proportional--derivative (PD) controller as shown in Equation~\eqref{eq:inner_position_loop}, which tracks the commanded trajectory $x_c$.

\begin{equation}
    u = k_{p}\,(x_{c} - x) + k_{v}\,(\dot{x}_{c} - \dot{x}),
    \label{eq:inner_position_loop}
\end{equation}

The inner loop has no knowledge of the desired impedance behaviour, its only purpose is to track the commanded position as accurate as possible. Therefore it must be sufficiently stiff in order for the admittance controller to work as intended. If the position control loop is compliant, the actual robot position will not accurately follow $x_c$. Consequently, the impedance felt by the operator becomes a combination of the outer impedance loop and the inner position loop, possibly creating unsafe behaviour \cite{al-shukaActiveImpedanceControl2018, songImpedanceControlRobots2017, andersonHybridImpedanceControl1988, songTutorialSurveyComparison2019}.

\subsubsection{Position--based versus velocity--based inner loop}
The inner loop can track either a commanded position $x_c$ or a commanded velocity $\dot{x}_c$. This choice depends on the application, which can be illustrated by considering a step force applied to the robotic structure by the human operator. In collaborative load handling, there is no spring that pulls the robot arm back to a static reference position. The human operator moves the robotic structure freely and expects it to stay wherever it is released, instead of pulling back. Therefore, in this example, the desired spring constant $k_d = \SI{0}{\newton\per\metre}$ to ensure that the robotic structure to not pull back against the force applied by the operator. To illustrate the difference between a position inner loop and a velocity inner loop, consider Equation~\eqref{eq:admittance_filter}, simplified by setting the desired inertia term $m_d$ and reference velocity $\dot{x}_r$ to zero. 
\begin{equation}
    b_{d}\,\dot{x}_{c} = f_{e}
    \label{eq:admittance_damping_only}
\end{equation}

The step force applied by the human operator $f_e$ is constant after $t = 0^{+}$, resulting in Equation~\eqref{eq:admittance_damping_only_cteforce} and~\eqref{eq:admittance_damping_only_xdot}.
\begin{equation}
    b_{d}\,\dot{x}_{c} = 1
    \label{eq:admittance_damping_only_cteforce}
\end{equation}
\begin{equation}
    \dot{x}_{c} = 1/b_{d}
    \label{eq:admittance_damping_only_xdot}
\end{equation}
A constant force will result in a constant velocity $\dot{x_c}$. Consequently, the commanded reference position $x_c$ grows linearly over time. This behaviour results from the absence of the virtual spring which would create an equilibrium position. With a position--based inner loop, the controller tries to track this continuously growing commanded position, meaning that any lag in the inner loop can cause accumulation of the tracking error. Therefore, this approach requires aggressive tuning, which is more prone to instability issues.

With a velocity--based inner loop, the controller tracks a commanded reference velocity $\dot{x_c}$ instead. Since $\dot{x_c}$ is constant for the theoretical step force applied by the operator, the controller tries to match a constant velocity instead of a continuously growing position. This approach allows for less aggressive tuning, and thus a more stable controller.

The mass--spring--damper system with the commanded trajectory $x_c$ is shown in Figure~\ref{fig:admittance_msd}. The admittance controller combines the measured force $f_e$ with the reference position $x_r$. When the robotic structure should remain in continuous contact with the environment, such as in grinding applications, $x_r$ can be set slightly beyond the contact surface to maintain a constant contact force \cite{al-shukaActiveImpedanceControl2018, songImpedanceControlRobots2017, andersonHybridImpedanceControl1988, songTutorialSurveyComparison2019}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{Admittanceschematic}
    \caption{Mass--spring--damper system with commanded position $x_c$ \cite{al-shukaActiveImpedanceControl2018}.}
    \label{fig:admittance_msd}
\end{figure}

\subsubsection{Comparison of Force-Based and Position-Based Impedance Control}
\label{subsubsec:impedance_comparison}

Force--based impedance control commands torques directly and does not require an inner control loop. It can operate without a force sensor when the desired inertia is set equal to the actual inertia. To achieve stiff behaviour, high values for the impedance parameters must be chosen, amplifying the sensor noise which can result in instabilities. As was stated before, either the environment or the robotic structure should be compliant. If the environment is stiff, the robotic structure must feel soft, meaning lower values for the impedance parameters can be chosen resulting in less sensor noise amplification.

Admittance control requires both a force sensor and a stiff inner control loop. It is consequently better suited for stiff behaviour against compliant environments, such as a human operator. The performance depends on the quality of the inner loop and the force measurement. Most industrial manipulators are equipped motor drivers with integrated position controllers, and since admittance control uses an inner position loop, it can be implemented without redesigning the low-level control architecture \cite{al-shukaActiveImpedanceControl2018, songImpedanceControlRobots2017, andersonHybridImpedanceControl1988, songTutorialSurveyComparison2019}.
\newpage

\subsection{Series elastic actuator}

In conventional machine design, the interface between a motor and its load is made as stiff as possible. As discussed previously, robotic systems are still predominantly used for position-control tasks, where high structural stiffness is beneficial. A stiff structure improves position accuracy, stability, and bandwidth for position control.
 
When only the motor position is measured, a stiff structure minimises the position error between the motor shaft and the endpoint. For example, if a motor is connected to a rigid shaft as shown in Figure~\ref{fig:sea_rigid}, the angular position of the endpoint can be accurately represented by the motor encoder reading. When this shaft is replaced by a compliant component such as a rubber coupling, as shown in Figure~\ref{fig:sea_coupling}, the motor encoder no longer accurately reflects the angular position of the endpoint.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{searigid}
    \caption{Motor and load connected with a rigid shaft.}
    \label{fig:sea_rigid}
\end{figure}
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{seawithspring}
    \caption{Motor and load connected with a compliant coupling.}
    \label{fig:sea_coupling}
\end{figure}

When the position sensor is placed after the elastic coupling, the elasticity becomes part of the feedback loop. The motor and load inertia are coupled through the elastic element, and a stiffer coupling increases the resonance frequency of the system, allowing a higher control bandwidth:

\begin{equation}
    \omega_n = \sqrt{k \left(\frac{1}{J_\text{motor}} + \frac{1}{J_\text{load}}\right)}
    \label{eq:sea_resfreq}
\end{equation}

Most electric motors have poor torque density: they can only achieve high power output at high rotational speeds. Since mechanical power is the product of torque and angular velocity.

\begin{equation}
    P = \tau \cdot \omega
    \label{eq:motor_power}
\end{equation}

A lightweight, high--speed motor requires a transmission to deliver useful torque at the low speeds typical of robotic joints. Unfortunately, gears introduce significant drawbacks such as backlash, friction, torque ripple, and reflected inertia. For a gear ratio of $N{:}1$, the reflected inertia at the output is

\begin{equation}
    J_{\text{reflected}} = J_{\text{motor}} \cdot N^{2}
    \label{eq:reflected_inertia}
\end{equation}

This $N^2$ amplification means that shock loads on the output shaft produce very high forces on the gear teeth, and it is often the load rating of the gears that limits the allowable output torque. The high reflected inertia also makes the actuator difficult to backdrive, which is problematic for force control and safe interaction with unknown environments. Because of this poor torque density at low speeds, motors with a peak power rating far exceeding their useful continuous output must be selected, resulting in oversized and expensive designs \cite{prattSeriesElasticActuators1995, prattSeriesElasticActuators2002, huResearchVariableImpedance2025b, yuLearningElasticitySeriesElastic2017,hurst}.

\subsubsection{The series elastic concept}
 
A solution to these problems was introduced by Pratt and Williamson, who proposed the Series Elastic Actuator (SEA). As the name describes, an elastic element is placed in series between the transmission output and the load, as illustrated in Figure~\ref{fig:sea_general}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{generalsea}
    \caption{General SEA schematic reconstructed from \cite{prattSeriesElasticActuators1995}.}
    \label{fig:sea_general}
\end{figure}

The spring obeys Hooke's law,
\begin{equation}
    F = k \cdot \delta x
    \label{eq:hooke}
\end{equation}
so by measuring the spring deflection $\delta x$ and knowing the spring constant $k$, the force transmitted to the load can be calculated directly.

Measuring force through a stiff transmission is inherently difficult; friction, backlash, and torque ripple influence the relationship between motor current and output force. The series elastic element resolves this by converting the force measurement into a displacement measurement. The output force is determined by the spring deflection, which is a position that can be measured accurately regardless of the transmission's non--linearities. The motor receives a torque command, but the feedback signal that signals the controller whether the output force is correct is the spring deflection which is a position measurement. In this way, the force sensing problem is transformed into a position sensing problem. A force control problem consequently becomes a position control problem, which is the strength of motor equiped with a transmission \cite{prattSeriesElasticActuators1995, prattSeriesElasticActuators2002, huResearchVariableImpedance2025b, yuLearningElasticitySeriesElastic2017,hurst}.

\subsubsection{Benefits of series elasticity}
 
The spring acts as a mechanical low--pass filter on shock loads. When the output is subjected to a sudden impact, the spring deflects and spreads the impulse over a longer duration, reducing the peak forces on the gear teeth. Only the output--side mass contributes to the initial impact, the reflected inertia $N^2 J_{\text{motor}}$ is decoupled from the output by the spring. The lengthened impulse also gives the control system more time to accelerate the motor inertia and respond to the external force.

In a stiff actuator with force feedback, even a small displacement generates a large force reading. A high--gain feedback controller would respond aggressively, pulling back and causing the force to drop suddenly, resulting in chatter between the actuator and the load. To avoid this, the controller gains must be kept low, resulting in a slow system that is unable to respond to small forces.

With the introduction of a compliant element a moderate displacement generates a small force reading. This allows higher control gains without risking instability. At frequencies within the control bandwidth, the force--feedback loop can effectively cancel the spring force entirely, creating the behaviour of a zero-rate spring (an infinite compliant spring). From Equation~\eqref{eq:sea_resfreq}, it is clear that when $k = \SI{0}{\newton\per\metre}$, the resonance frequency $\omega_n = \SI{0}{\radian\per\second}$. Since motor and load inertias cannot resonate with a zero--rate spring, the system is inherently stable when interacting with any passive object.

At high frequencies, the impedance of a traditional stiff actuator is dominated by its reflected inertia, while a SEA presents the impedance of the spring, which is lower and therefore safer for interactions with unknown environments. However, the series spring introduces two distinct resonance phenomena that are relevant to the control design. The first is the motor--load resonance frequency, given by Equation~\ref{eq:sea_resfreq}. For a rigid transmission this frequency is high, which is beneficial for position control bandwidth, as a stiffer coupling allows the motor to drive the load at higher frequencies before the two inertias begin to move independently. Introducing the series spring lowers this resonance frequency, reducing the position control bandwidth of the motor--load system relative to a rigid transmission; this is the fundamental bandwidth cost of the SEA. The second resonance occurs between the spring and the load--side inertia alone, and determines the high--frequency output impedance seen by the environment. Above this resonance frequency, the spring can no longer transmit force effectively and the load behaves as a free mass. For safe interaction with a human operator, this resonance should fall within the control bandwidth so the controller can actively shape the apparent impedance across the relevant frequency range. In practice, the load--side inertia of the robotic structure is large enough that this resonance frequency falls well within the operational bandwidth of the controller, allowing the force feedback loop to manage it effectively. A very light load would push this resonance to a higher frequency, potentially outside the control bandwidth, limiting the controller's ability to damp it.

Because the force output of a SEA depends on the spring deflection between the motor--side and load--side positions rather than the motor's internal torque, the transmission's non--linearities (such as friction, backlash, and torque ripple) do not directly affect the force measurement. These non--linearities affect the motor effort required to achieve a given shaft position, but since the force is measured via the spring deflection and incorporated in the feedback loop, the controller commands the correct motor motion regardless. This allows the use of less expensive gears and motors without sacrificing force--control quality. \cite{prattSeriesElasticActuators1995, prattSeriesElasticActuators2002, huResearchVariableImpedance2025b, yuLearningElasticitySeriesElastic2017,hurst}.
\newpage

\subsubsection{Performance limits}
The performance limits of a series elastic actuator can be understood by decomposing the required motor force. Consider the system shown in Figure~\ref{fig:seaModel}, where a motor with mass $M_m$ is connected to the load through a spring with stiffness $k_s$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{sea_spring_motormass}
    \caption{Series elastic actuator model reconstructed from \cite{prattSeriesElasticActuators1995}.}
    \label{fig:seaModel}
\end{figure}
 
The spring force on the load is given by Hooke's law:
\begin{equation}
    F_l = k_s (x_m - x_l)
    \label{eq:sea_spring}
\end{equation}
where $x_m$ is the motor position and $x_l$ is the load position. Newton's second law for the motor gives the net force to accelerate the motor mass:
\begin{equation}
    M_m \ddot{x}_m = F_m - F_l
    \label{eq:sea_newton}
\end{equation}
Rearranging:
\begin{equation}
    F_m = F_l + M_m \ddot{x}_m
    \label{eq:sea_fm_basic}
\end{equation}
The motor position can be expressed in terms of the load position and the spring force by rearranging Equation~\eqref{eq:sea_spring}:
\begin{equation}
    x_m = x_l + \frac{F_l}{k_s}
    \label{eq:sea_xm}
\end{equation}
Differentiating twice:
\begin{equation}
    \ddot{x}_m = \ddot{x}_l + \frac{\ddot{F}_l}{k_s}
    \label{eq:sea_xm_ddot}
\end{equation}

Substituting into Equation~\eqref{eq:sea_fm_basic}:
\begin{equation}
    F_m = F_l + M_m \ddot{x}_l + \frac{M_m}{k_s} \ddot{F}_l
    \label{eq:sea_fm_time}
\end{equation}

To analyze the system in the frequency domain, sinusoidal steady--state signals are assumed. Any sinusoidal signal can be represented using a complex exponential of the form:
\begin{equation}
    x(t) = \hat{x} e^{j\omega t}, \quad F(t) = \hat{F} e^{j\omega t}
\end{equation}
where $\hat{x}$ and $\hat{F}$ are complex amplitudes and $\omega$ is the angular frequency.

Using the complex exponential form allows derivatives to be computed easily. In particular, differentiating $e^{j\omega t}$ results in multiplication by $j\omega$, and the second derivative results in multiplication by $-\omega^2$.
\begin{equation}
    \frac{d}{dt} e^{j\omega t} = j\omega e^{j\omega t}, \quad
    \frac{d^2}{dt^2} e^{j\omega t} = -\omega^2 e^{j\omega t}
\end{equation}

Therefore, the second derivatives can be replaced as:
\begin{equation}
    \ddot{x} \rightarrow -\omega^2 x, \quad \ddot{F} \rightarrow -\omega^2 F
\end{equation}

Applying these substitutions to Equation~\eqref{eq:sea_fm_time} gives:
\begin{equation}
    \boxed{F_m = F_l - \frac{M_m \omega^2}{k_s} F_l - M_m \omega^2 x_l}
    \label{eq:sea_fm_freq}
\end{equation}
 
The three terms have the following physical interpretation:
\begin{enumerate}
    \item $F_L$: $F_l$ is the force applied to the load coming from the spring.
    \item $F_ms$: $\dfrac{M_m \omega^2}{k_s} F_l$ is force used to accelerate the motor mass, which in turn will cause spring deformation. This term is unique to the SEA.
    \item $F_mi$: $M_m \omega^2 X_l$ is the force required to accelerate the motor mass to track a moving load. This term exist regardless of the presence of a series spring.
\end{enumerate}

The force that is required by the motor is limited to $F_{\text{max}}$, which in practise can be a limit on the motor current to prevent overheating for example.

\begin{equation}
    \left| F_m \right| \leq F_{\max}
\end{equation}

The motor force constraint $|F_m| \leq F_{\max}$ can be visualised as a vector diagram (Figure~\ref{fig:VectorSEA}). The vector $F_m$ is the sum of three components, and its magnitude must stay inside a circle of radius $F_{\max}$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{sea_vector1}
    \caption{Vector diagram reconstructed from \cite{prattSeriesElasticActuators1995}.}
    \label{fig:VectorSEA}
\end{figure}

$F_l$ is arbitrarely aligned with the real axis. The second component (the SEA term) points in the opposite direction along the real axis, moving the starting point of the third vector to $F_l(1 - M_m\omega^2/K_s)$. From this new starting point, the third component ($-M_m\omega^2 X_l$) extends in the direction determined by the phase relationship between force and load motion. The final sum gives $F_m$.
 
The series elasticity is beneficial for all frequencies $\omega < \sqrt{2\frac{k_s}{M_m}}$ given by Equation~\eqref{eq:sea_vector_limit}, since the vector $F_m$ gets smaller. The actuator can handle faster or larger load motions before saturating at $F_{\text{max}}$.
\begin{equation}
    \left|1 - \frac{M_m \omega^2}{k_s}\right| < 1
    \label{eq:sea_vector_condition}
\end{equation}
Expanding the absolute value inequality:
\begin{equation}
    -1 < 1 - \frac{M_m \omega^2}{k_s} < 1
\end{equation}
The right--hand side gives $ \frac{M_m \omega^2}{k_s} \ge 0 $, which is always satisfied since $M_m > 0$, $k_s > 0$, and $\omega^2 \ge 0$. The left--hand side gives:
\begin{equation}
    \frac{M_m \omega^2}{k_s} < 2 \quad \Longrightarrow \quad \omega < \sqrt{\frac{2 k_s}{M_m}}
    \label{eq:sea_vector_limit}
\end{equation}

The series elasticity improves or limits performance depends on the task. When the output force varies, the motor must move to change the spring deflection, and the motor's inertia and torque limits restrict how quickly this can happen and thus limit the bandwidth in which the actuator can guarantee a zero--rate spring. When a time--varying force $F(t)$ is commanded, the required motor position is $x_m(t) = x_l(t) + F(t)/k$, since the spring deflection $F(t)/k$ must be added to the current load position $x_l(t)$ to achieve the desired force. The load motion $x_l(t)$ can therefore add constructively or destructively to the required spring deflection, depending on the relative amplitude and phase between the commanded force and the load motion. When the load moves in a direction that naturally compresses the spring in the same direction as the desired force change, less motor motion is required and the effective bandwidth increases. When the load moves in the opposite direction, more motor motion is needed and bandwidth decreases

The practical consequence for actuator design is that the spring stiffness should be chosen to match the task. For tasks requiring a constant force against a moving load, a softer spring causes small force errors and thus less motor effort. For tasks requiring varying forces on a stationary load, a stiffer spring is better because motor torque is transmitted more directly. No single spring stiffness is optimal for all tasks \cite{prattSeriesElasticActuators1995, prattSeriesElasticActuators2002, huResearchVariableImpedance2025b, yuLearningElasticitySeriesElastic2017,hurst}.
\newpage

\subsubsection{Control architectures}

Pratt and Williamson proposed a control architecture that combines feedforward compensation with feedback correction. The architecture is shown in Figure~\ref{fig:sea_pratt}, where the transfer functions are derived from Equation~\eqref{eq:sea_fm_freq} by using $s = j\omega \rightarrow \omega^2 = -s^2$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{seacontrolscheme}
    \caption{Torque control diagram with feedforward terms reconstructed \cite{prattSeriesElasticActuators1995}.}
    \label{fig:sea_pratt}
\end{figure}

The controller receives a desired output force $F_d$ and the measured spring force $F_l = k_s(x_m - x_l)$. The controller's task is to make $F_l$ track $F_d$.

The force error $F_d - F_l$ is processed by a PID feedback loop. Feedforward terms compensate for the components of the required motor force derived in Equation~\eqref{eq:sea_fm_freq}. The first two terms, the desired output force and the motor acceleration needed to change the spring deflection, are fully compensated in the feedforward path. The third term, motor acceleration to track load motion, is only partially compensated by choosing a $K_b < 1$ to prevent instability. The combined feedforward and feedback output is a desired motor current, which translates to a motor torque \cite{prattSeriesElasticActuators1995}.
\newpage

Yu et al.\ proposed a cascaded control architecture consisting of three nested loops~\cite{yuLearningElasticitySeriesElastic2017}. The architecture is shown in Figure~\ref{fig:yuetalscheme}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{yuetal}
    \caption{Torque control diagram from \cite{yuLearningElasticitySeriesElastic2017}.}
    \label{fig:yuetalscheme}
\end{figure}
 
The outermost layer is the spring model, which converts the desired torque $\tau_{\text{des}}$ into a desired spring deflection $\delta_{\text{des}}$. For a linear spring, this conversion is:
\begin{equation}
    \delta_{\text{des}} = \frac{\tau_{\text{des}}}{K_s}
    \label{eq:sea_defl_linear}
\end{equation}

When the spring exhibits nonlinear behaviour such as hysteresis, a data--driven model can be used instead. Yu et al.\ created their own model to better model the spring behaviour.
 
The middle loop is a PID controller that drives the measured spring deflection $\delta$ towards the desired deflection $\delta_{\text{des}}$. The spring deflection is computed as the difference between two absolute encoders placed on either side of the elastic element. The output of this deflection controller is a motor current command.
 
The innermost loop is a motor current controller (PID), which regulates the actual motor current to match the commanded value.
 
Compared to the Pratt and Williamson architecture, the cascaded approach does not use explicit feedforward compensation for motor inertia or load motion. Instead, these effects are handled implicitly by the deflection PID. If friction or inertia causes the actual deflection to deviate from the desired value, the deflection controller increases the motor current until the error is corrected. The absence of feedforward terms makes the controller simpler to implement, at the cost of relying entirely on the feedback gains to reject disturbances \cite{prattSeriesElasticActuators1995, prattSeriesElasticActuators2002, huResearchVariableImpedance2025b, yuLearningElasticitySeriesElastic2017,hurst}.


% ============================================================
\chapter{Methodology}
\label{ch:methodology}
% ============================================================

\section{System Overview}

This thesis presents the design and development of a testbench for a 3--DOF collaborative robot arm with novel elastic transmission joints. The physical system has been fully implemented, with all mechanical and electronic components integrated and ready for deployment. The arm is intended for assisted load handling tasks, where integrated spring elements reduce operator effort while maintaining safe human--robot interaction.

In addition, a control strategy is proposed and implemented in a standalone C++ environment. A simulation environment in ROS 2 was developed to support the implementation and evaluation of different control strategies and robot structures. To ensure portability and ease of deployment, Docker is used to provide a consistent and reproducible setup across different systems. The resulting testbench provides a robust platform for experimental validation and analysis of the novel transmission in practical applications.

This master thesis has four main objectives:

\begin{enumerate}
    \item \textbf{Testbench with Direct--Drive Actuation}\\
    The first objective is to design and construct a testbench that enables accurate characterization of the novel transmission dynamics. A direct--drive architecture is adopted, in which the motor--transmission units are connected directly to the joints without intermediate elements such as pulleys, thereby avoiding additional compliance that could obscure the intrinsic spring behavior. The mechanical design comprises two actuated joints with integrated elastic elements and one passive joint rotating about the z--axis.

    \item \textbf{Hardware and Sensor Architecture}\\
    The second objective is the careful selection and integration of electronic components required for reliable data acquisition. The microcontroller interface, motor drivers, and position sensors must meet timing requirements to support operation at a 1\,kHz control frequency.
    
    \item \textbf{Simulation Environment}\\
    The third objective is to develop a simulation environment in ROS~2 that enables controller design and testing prior to hardware commissioning. The simulation serves two main purposes. First, it facilitates iterative development of control algorithms with full system visualization. Second, it provides insight into system behavior, which proved valuable during the early design phase for identifying and tuning soft joint limits.

    Using URDF descriptions and trajectory specifications provided via YAML files, the simulator predicts the arm's behavior through inverse dynamics. It generates the corresponding torque commands and exports state data such as position, velocity, acceleration, and required torques in CSV format for analysis and validation. This framework provides a systematic approach to controller design independent of hardware availability.

    \item \textbf{Control Strategy for Assistive Weight Compensation}\\
    The fourth objective is to develop a control strategy that leverages the elastic transmission for weight compensation. The strategy uses feedback from the spring deflection to estimate and compensate for gravitational loads, thereby reducing operator effort during manual manipulation.
\end{enumerate}

\newpage
\section{Mechanical Design}
 
\subsection{General overview}
 
The testbench shown in Figure~\ref{fig:Testbench_overview} consists of a 3--DOF robotic arm with two active joints (shoulder joint shown in green, and elbow joint shown in orange) and one passive joint (rotation about the z--axis). This configuration was chosen to first validate the novel transmission design with two units before considering a third actuated joint. The passive joint maintains mechanical simplicity and leaves room for future extension with a third transmission mounted at the base for autonomous movement. A simple end-effector, shown in red, allows weights to be suspended from the arm.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{OverviewWithAxisSET}
    \caption{Testbench overview with transmissions blurred to protect intellectual property.}
    \label{fig:Testbench_overview}
\end{figure}

An overview of the testbench annotated with overall lengths is shown in Figure~\ref{fig:Testbench_overview_length}. The distance from the shoulder joint axis to the elbow joint axis is \SI{200}{\milli\meter}, while the distance from the elbow joint to the load fixture (shown in red) is identical. The total height of the base is \SI{236}{\milli\meter}. Figure~\ref{fig:NumbersOverview} provides a labeled side--view overview of the testbench components.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{LengthOverview}
    \caption{Testbench overview with overall dimensions}
    \label{fig:Testbench_overview_length}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{NumbersOverview}
    \caption{Labeled side--view overview of the testbench}
    \label{fig:NumbersOverview}
\end{figure}

\subsection{Design Motivation and Constraints}

The most critical mechanical design constraint is the maximum allowable internal torque of the compliant transmission, which is \SI{16}{\newton\meter}, as it determines the maximum allowable output torque. The selection of the couplings, the lengths of the robotic links, and the allowable payload at the end effector are all defined based on this torque limit. The design choices are justified in the following order:

COMMENT: we should change this list to match the following chapters

\begin{enumerate}
    \item Material selection and analysis of the joint shafts
    \item Design and analysis of the joint fixtures
    \item Design and analysis of the link structures
    \item Design of the passive joint
    \item Design of the base
    \item Elbow joint limit
    \item Design of end effector
\end{enumerate}

Each active joint employs the novel elastic transmission. Two transmission sizes were implemented. The larger shoulder joint uses a transmission with a reduction ratio of 299:1, while the elbow joint uses a smaller 150:1 transmission, since its distance to the end effector is half that of the shoulder joint. This configuration enables evaluation of two different elastic transmission sizes within the same testbench.

Two Maxon brushless DC motors are used with the specifications provided in Appendix~\ref{app:MaxonMotorDatasheet}:
\begin{itemize}
	\item Nominal (max.~continuous) torque: \SI{0.221}{\newton\meter}
	\item Stall torque: \SI{1.14}{\newton\meter}
\end{itemize}
 
The motor drivers are ESCON2 Compact 60/12 with a maximum continuous output current of 12\,A and peak current capability of 24\,A for durations less than 10\,s as shown in Appendix~\ref{app:ESCON2Datasheet}.The motor stall torque of \SI{1.140}{\newton\meter} is achieved at a stall current of 51.4\,A. However, the ESCON2 driver can only provide a maximum peak current of 24\,A for durations less than \SI{10}{\second}. Therefore, the peak torque that can be delivered is limited by the driver current:

\begin{equation}
\tau_{\text{motor, peak}} = \tau_{\text{stall}} \cdot \frac{I_{\text{peak}}}{I_{\text{stall}}} = 1.14\,\text{Nm} \cdot \frac{24}{51.4} \approx 0.534\,\text{Nm}
\end{equation}

With transmission ratio $r$, this corresponds to a peak output shaft torque of:

\begin{equation}
\tau_{\text{output, peak}} = \tau_{\text{motor, peak}} \cdot r = 0.534\,\text{Nm} \cdot 299
\approx 160\,\text{Nm}
\end{equation}

At nominal operating conditions, the motors delivers a continuous torque of 221\,mNm at 3.53\,A. With the transmission reduction ratio of $r = 299$ for the shoulder joint, the nominal output shaft torque is:

\begin{equation}
\tau_{\text{output, nominal}} = \tau_{\text{motor, nominal}} \cdot r = 221\,\text{mNm} \cdot 299 \approx 66\,\text{Nm}
\end{equation}

This nominal torque represents the maximum sustained torque that can be continuously applied without overheating of the motor. Both the nominal and peak motor output torques exceed the \SI{16}{\newton\meter} transmission limit. Safe operation therefore requires software torque limitation, which can be implemented in the configuration of the ESCON2 motor drivers, and in the control loop.
\subsection{Parallel Key Verification According to DIN 6892}
\label{subsec:key_verification}
 
A direct--drive approach is chosen so that no compliant elements like toothed belts can mask the behaviour of the series elastic transmission. The output shafts are thus rigidly connected to the joint shafts by means of a rigid coupling. A KBK KBST2 size~8 rigid clamping coupling with a parallel key according to DIN~6885 was selected, since these are readily available standard parts. The transmittable torque of the coupling is \SI{29}{\newton\metre} for the $\varnothing$8/$\varnothing$8 bore configuration (see Appendix~\ref{app:KBST2Datasheet}). The strength of the parallel key connection is verified using DIN~6892, Method~C (simplified surface pressure calculation). A minimum safety factor of $S \geq 1.5$ is adopted. Three configurations are evaluated: the DIN--standard $2 \times 2$\,\si{\milli\metre} key in 1.4401 stainless steel, an oversized $3 \times 3$\,\si{\milli\metre} key in 1.4401 stainless steel, and the same oversized key in 1.7225 alloy steel (42CrMo4) as a potential upgrade~\cite{ParallelKeysSelectable}.
 
\subsubsection{Loading Conditions}
\label{subsec:loading_conditions}
 
The total torque at the joint consists of two components: the static gravitational torque and the dynamic torque required to accelerate the arm.
 
\paragraph{Static torque.}
The worst--case static loading occurs when the robot arm is fully stretched horizontally. Although this configuration is not reachable due to the physical joint limits, it is nonetheless considered as a worst--case scenario. The total mass of the elbow joint assembly, including the transmission, motor, and cables, is approximately \SI{1.2}{\kilogram} at a distance of \SI{0.2}{\metre} from the shoulder axis. The payload at the end effector is set to \SI{1.0}{\kilogram} at \SI{0.4}{\metre} --- sufficient for gravity compensation to be perceptible to the human operator, while limiting mechanical loading on the structure. The gravitational torque at the shoulder joint is:
 
\begin{equation}
    T_{\text{static}} = \sum_{i} m_i \cdot g \cdot r_i = 1.2 \cdot 9.81 \cdot 0.2 + 1.0 \cdot 9.81 \cdot 0.4 = \SI{6.28}{\newton\metre}
    \label{eq:static_torque}
\end{equation}
 
\paragraph{Dynamic torque.}
To accelerate the arm from rest, additional torque is required to overcome the mass moment of inertia of the links. The moment of inertia of the arm about the shoulder joint, concidered as point masses at their respective centres of gravity, is:
 
\begin{equation}
    J_{\text{arm}} = \sum_{i} m_i \cdot r_i^2 = 1.2 \cdot 0.2^2 + 1.0 \cdot 0.4^2 = 0.048 + 0.160 = \SI{0.208}{\kilogram\metre\squared}
    \label{eq:arm_inertia}
\end{equation}

The torque transmitted through the coupling is determined by the arm--side inertia and the gravitational load. An angular acceleration of $\alpha = \SI{5}{\radian\per\second\squared}$ is adopted, corresponding to a $90^\circ$ sweep from rest in approximately \SI{0.8}{\second}. The dynamic torque contribution is:
 
\begin{equation}
    T_{\text{dyn}} = J_{\text{arm}} \cdot \alpha = 0.208 \cdot 5 = \SI{1.04}{\newton\metre}
    \label{eq:dynamic_torque_calc}
\end{equation}
 
\paragraph{Nominal torque.}
The total nominal torque at the shaft--key connection is the sum of the static and dynamic components:
 
\begin{equation}
    T_{\text{nom}} = T_{\text{static}} + T_{\text{dyn}} = 6.28 + 1.04 = \SI{7.32}{\newton\metre}
    \label{eq:nominal_torque}
\end{equation}
 
\subsubsection{Input Parameters}
 
According to DIN~6885:1968-08, the standard key for a shaft diameter of $d = \SI{8}{\milli\metre}$ is $b \times h = 2 \times 2$\,\si{\milli\metre}, with keyway depths $t_1 = \SI{1.2}{\milli\metre}$ (shaft) and $t_2 = \SI{1.0}{\milli\metre}$ (hub). However, an oversized $3 \times 3$\,\si{\milli\metre} key was selected to improve the safety margins. The relevant geometric and material parameters for all three cases are summarised in Tables~\ref{tab:key_input_common} and~\ref{tab:key_geometry}. The application factor $K_A = 1.25$ is taken from DIN~3990-1, Table~A1, for a uniform driving machine (electric motor) with light shocks on the driven side~\cite{16ParallelKeys}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l l l}
        \hline
        \textbf{Parameter} & \textbf{Symbol} & \textbf{Value} \\
        \hline
        \multicolumn{3}{l}{\textit{Torque and calculation factors}} \\
        Nominal torque          & $T_{\text{nom}}$  & \SI{7.32}{\newton\metre} \\
        Application factor & $K_A$  & 1.25 \\
        Load factor             & $K_v$             & 1.0 \\
        Number of keys          & $i$               & 1 \\
        \hline
        \multicolumn{3}{l}{\textit{Shaft~\cite{CNCMachiningTool}}} \\
        Diameter                & $d$               & \SI{8}{\milli\metre} \\
        Yield strength          & $R_{p0.2}$        & \SI{2050}{\mega\pascal} \\
        Support factor          & $f_S$             & 1.3 \\
        Hardness influence factor & $f_H$           & 1.0 \\
        \hline
    \end{tabular}
    \caption{Common input parameters for parallel key verification.}
    \label{tab:key_input_common}
\end{table}
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l c c c}
        \hline
        \textbf{Parameter} & \textbf{Case 1} & \textbf{Case 2} & \textbf{Case 3} \\
         & \textbf{2$\times$2, 1.4401} & \textbf{3$\times$3, 1.4401} & \textbf{3$\times$3, 1.7225} \\
        \hline
        Width $b$ [\si{\milli\metre}]              & 2     & 3     & 3 \\
        Height $h$ [\si{\milli\metre}]             & 2     & 3     & 3 \\
        Supporting length $l_{tr}$ [\si{\milli\metre}] & 14    & 14    & 14 \\
        Shaft keyway depth $t_1$ [\si{\milli\metre}]   & 1.2   & 1.8   & 1.8 \\
        Hub keyway depth $t_2$ [\si{\milli\metre}]     & 1.0   & 1.4   & 1.4 \\
        Material                    & 1.4401 & 1.4401 & 1.7225 \\
        $R_{p0.2}$ [\si{\mega\pascal}]           & 200   & 200   & 500 \\
        $R_m$ [\si{\mega\pascal}]                 & 500--700 & 500--700 & 750--1300 \\
        $f_S$                       & 1.1   & 1.1   & 1.1 \\
        $f_H$                       & 1.0   & 1.0   & 1.0 \\
        \hline
    \end{tabular}
    \caption{Key geometry and material parameters for the three configurations.}
    \label{tab:key_geometry}
\end{table}
 
\subsubsection{Equivalent Torque}
 
The equivalent torque accounts for the application factor:
 
\begin{equation}
    T_{\text{eq}} = K_A \cdot T_{\text{nom}} = 1.25 \cdot 7320 = \SI{9.15}{\newton\metre}
    \label{eq:equivalent_torque}
\end{equation}
 
\subsubsection{Surface Pressure}
 
According to DIN~6892 Method~C, the surface pressure on the shaft and hub keyway walls is:
 
\begin{equation}
    p = \frac{2 \cdot T_{\text{eq}}}{d \cdot t \cdot l_{tr} \cdot i \cdot K_v}
    \label{eq:surface_pressure}
\end{equation}
 
\subsubsection{Permissible Surface Pressure}
 
For ductile materials, the permissible surface pressure is given by:
 
\begin{equation}
    p_{\text{zul}} = f_S \cdot f_H \cdot R_{p0.2}
    \label{eq:p_permissible}
\end{equation}
 
The permissible values for the key and shaft are listed in Table~\ref{tab:p_zul}. The coupling hub is not included in the verification as its capacity is specified by the manufacturer at \SI{29}{\newton\metre}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l c c c c}
        \hline
        \textbf{Component} & $R_{p0.2}$ [\si{\mega\pascal}] & $f_S$ & $f_H$ & $p_{\text{zul}}$ [\si{\mega\pascal}] \\
        \hline
        Key --- 1.4401      & 200   & 1.1 & 1.0 & 220  \\
        Key --- 1.7225     & 500   & 1.1 & 1.0 & 550  \\
        Shaft --- S7        & 2050  & 1.3 & 1.0 & 2665 \\
        \hline
    \end{tabular}
    \caption{Permissible surface pressure for key and shaft.}
    \label{tab:p_zul}
\end{table}
 
\subsubsection{Shaft Torsion Check}
 
The keyway causes stress concentration. A torsional stress concentration factor of $K_{ts} = 3.0$ is adopted from Peterson's Stress Concentration Factors (see Appendix~\ref{app:peterson}). This check is common to all three cases, as it depends only on the shaft geometry and material:
 
\begin{equation}
    W_p = \frac{\pi \cdot d^3}{16} = \frac{\pi \cdot 8^3}{16} = \SI{100.5}{\milli\metre\cubed}
    \label{eq:polar_modulus}
\end{equation}
 
\begin{equation}
    \tau_{\text{nom}} = \frac{T_{\text{eq}}}{W_p} = \frac{9150}{100.5} = \SI{91}{\mega\pascal}
    \label{eq:torsion_stress}
\end{equation}
 
\begin{equation}
    \tau_{\text{eff}} = K_{ts} \cdot \tau_{\text{nom}} = 3\cdot 91 = \SI{273}{\mega\pascal}
    \label{eq:torsion_stress_eff}
\end{equation}
 
\begin{equation}
    \tau_{\text{allow}} = \frac{R_{p0.2}}{\sqrt{3}} = \frac{2050}{1.732} = \SI{1183}{\mega\pascal}
    \label{eq:torsion_allow}
\end{equation}
 
\begin{equation}
    S_{\text{torsion}} = \frac{\tau_{\text{allow}}}{\tau_{\text{eff}}} = \frac{1183}{273} = 4.3 \geq 1.5
    \label{eq:torsion_sf}
\end{equation}

\subsubsection{Case 1: $2 \times 2$\,\si{\milli\metre} 1.4401 stainless steel key}
 
For the standard key dimensions ($t_1 = \SI{1.2}{\milli\metre}$, $t_2 = \SI{1.0}{\milli\metre}$), the surface pressures are:
 
\begin{equation}
    p_1 = \frac{2 \cdot 9150}{8 \cdot 1.2 \cdot 14} = \frac{18300}{134.4} = \SI{136}{\mega\pascal}
    \label{eq:p1_2x2}
\end{equation}
 
\begin{equation}
    p_2 = \frac{2 \cdot 9150}{8 \cdot 1.0 \cdot 14} = \frac{18300}{112.0} = \SI{163}{\mega\pascal}
    \label{eq:p2_2x2}
\end{equation}
 
The safety factors are presented in Table~\ref{tab:sf_2x2}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l l r r r l}
        \hline
        \textbf{Component} & \textbf{Side} & $p$ [\si{\mega\pascal}] & $p_{\text{zul}}$ [\si{\mega\pascal}] & $S$ & \textbf{Result} \\
        \hline
        Key (1.4401)    & shaft side & 136 & 220  & 1.62  & Pass \\
        Key (1.4401)    & hub side   & 163 & 220  & 1.35  & Fail \\
        Shaft (S7)      & shaft side & 136 & 2665 & 19.6  & Pass \\
        \hline
    \end{tabular}
    \caption{Safety factors --- DIN-standard $2 \times 2$\,\si{\milli\metre} 1.4401 key. Required: $S \geq 1.5$.}
    \label{tab:sf_2x2}
\end{table}
 
The maximum allowable nominal torque is governed by the key on the hub side:
 
\begin{equation}
    T_{\max} = \frac{p_{\text{zul,key}} \cdot d \cdot t_2 \cdot l_{tr}}{2 \cdot K_A} = \frac{220 \cdot 8 \cdot 1.0 \cdot 14}{2 \cdot 1.25} = \SI{9.9}{\newton\metre}
    \label{eq:t_max_2x2}
\end{equation}
 
The DIN-standard $2 \times 2$\,\si{\milli\metre} key \textbf{fails} on the hub side with a safety factor of 1.35, below the required 1.5. This motivates the selection of the oversized $3 \times 3$\,\si{\milli\metre} key.
 
\subsubsection{Case 2: $3 \times 3$\,\si{\milli\metre} 1.4401 stainless steel key}
 
With the larger key ($t_1 = \SI{1.8}{\milli\metre}$, $t_2 = \SI{1.4}{\milli\metre}$), the surface pressures reduce to:
 
\begin{equation}
    p_1 = \frac{2 \cdot 9150}{8 \cdot 1.8 \cdot 14} = \frac{18300}{201.6} = \SI{91}{\mega\pascal}
    \label{eq:p1_3x3_c45}
\end{equation}
 
\begin{equation}
    p_2 = \frac{2 \cdot 9150}{8 \cdot 1.4 \cdot 14} = \frac{18300}{156.8} = \SI{117}{\mega\pascal}
    \label{eq:p2_3x3_c45}
\end{equation}
 
The safety factors are presented in Table~\ref{tab:sf_3x3_c45}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l l r r r l}
        \hline
        \textbf{Component} & \textbf{Side} & $p$ [\si{\mega\pascal}] & $p_{\text{zul}}$ [\si{\mega\pascal}] & $S$ & \textbf{Result} \\
        \hline
        Key (1.4401)    & shaft side & 91  & 220  & 2.42  & Pass \\
        Key (1.4401)    & hub side   & 117 & 220  & 1.88  & Pass \\
        Shaft (S7)      & shaft side & 91  & 2665 & 29.3  & Pass \\
        \hline
    \end{tabular}
    \caption{Safety factors --- oversized $3 \times 3$\,\si{\milli\metre} 1.4401 key. Required: $S \geq 1.5$.}
    \label{tab:sf_3x3_c45}
\end{table}
 
The maximum allowable nominal torque is governed by the key on the hub side:
 
\begin{equation}
    T_{\max} = \frac{p_{\text{zul,key}} \cdot d \cdot t_2 \cdot l_{tr}}{2 \cdot K_A} = \frac{220 \cdot 8 \cdot 1.4 \cdot 14}{2 \cdot 1.25} = \SI{13.8}{\newton\metre}
    \label{eq:t_max_3x3_c45}
\end{equation}
 
All safety factors exceed the required minimum of~1.5. The maximum allowable torque of \SI{13.8}{\newton\metre} is below the failure threshold of the transmission keys (\SI{16}{\newton\metre}), ensuring that the shaft key acts as a sacrificial element that fails before the less accessible transmission components.
 
\subsubsection{Case 3: Oversized $3 \times 3$\,\si{\milli\metre} 1.7225 Key}
 
The surface pressures remain identical to Case~2 since the geometry is unchanged. Only the permissible surface pressure of the key increases. The resulting safety factors are shown in Table~\ref{tab:sf_3x3_42crmo4}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l l r r r l}
        \hline
        \textbf{Component} & \textbf{Side} & $p$ [\si{\mega\pascal}] & $p_{\text{zul}}$ [\si{\mega\pascal}] & $S$ & \textbf{Result} \\
        \hline
        Key (1.7225)   & shaft side & 91  & 550  & 6.04  & Pass \\
        Key (1.7225)   & hub side   & 117 & 550  & 4.70  & Pass \\
        Shaft (S7)      & shaft side & 91  & 2665 & 29.3  & Pass \\
        \hline
    \end{tabular}
    \caption{Safety factors --- oversized $3 \times 3$\,\si{\milli\metre} 1.7225 key. Required: $S \geq 1.5$.}
    \label{tab:sf_3x3_42crmo4}
\end{table}
 
The maximum allowable nominal torque is governed by the key on the hub side:
 
\begin{equation}
    T_{\max} = \frac{p_{\text{zul,key}} \cdot d \cdot t_2 \cdot l_{tr}}{2 \cdot K_A} = \frac{550 \cdot 8 \cdot 1.4 \cdot 14}{2 \cdot 1.25} = \SI{34.5}{\newton\metre}
    \label{eq:t_max_3x3_42crmo4}
\end{equation}
 
\subsubsection{Comparison and Design Choice}
\label{subsec:referToThisFea}
A summary comparing all three key configurations is given in Table~\ref{tab:key_comparison}.
 
\begin{table}[htbp]
    \centering
    \begin{tabular}{l c c c}
        \hline
        & \textbf{2$\times$2, 1.4401} & \textbf{3$\times$3, 1.4401} & \textbf{3$\times$3, 1.7225} \\
        \hline
        Minimum safety factor           & 1.35 (Fail)       & 1.88              & 4.70 \\
        Maximum allowable torque        & \SI{9.9}{\newton\metre}  & \SI{13.8}{\newton\metre}  & \SI{34.5}{\newton\metre} \\
        DIN 6892 at $T_{\text{nom}} = \SI{7.32}{\newton\metre}$ & Fail  & Pass              & Pass \\
        Fails before transmission keys (\SI{16}{\newton\metre}) & Yes    & Yes               & No \\
        \hline
    \end{tabular}
    \caption{Comparison of key configurations.}
    \label{tab:key_comparison}
\end{table}
 
The DIN-standard $2 \times 2$\,\si{\milli\metre} key is insufficient, with a safety factor of 1.35 on the hub side, which is below the required 1.5. The use of an oversized $3 \times 3$\,\si{\milli\metre} key, increases the minimum safety factor to 1.88.
 
The oversized $3 \times 3$\,\si{\milli\metre} key in 1.4401 stainless steel is selected for the current design. All safety factors exceed the required minimum of~1.5, and the maximum allowable torque of \SI{13.8}{\newton\metre} provides margin above the operating torque of \SI{7.32}{\newton\metre}. Crucially, the 1.4401 stainless steel key acts as a sacrificial element. It fails at \SI{13.8}{\newton\metre}, which is below the \SI{16}{\newton\metre} failure threshold of the transmission keys. This ensures that in an overload scenario, the easily replaceable shaft key fails before the less accessible transmission components.
 
Should future operating loads increase beyond the capacity of the 1.4401 stainless steel key, a 1.7225 alloy steel key can be used as replacement, raising the allowable torque to \SI{34.5}{\newton\metre}. The transmission and coupling should naturally match this increase in torque.


\subsection{Static Study on Shaft Shoulder Radius}
\label{subsec:shaft_static_study}
 
The shaft is supported on both sides by SKF bearings. The guidelines concerning shaft tolerances and dimensions were followed, resulting in a shoulder abutment radius of \SI{0.2}{\milli\metre}. 
This section examines the stress state at the shaft shoulder radius by means of a static finite element analysis, since the shoulder fillet is known to cause stress concentration.
 
\subsubsection{Model Setup}
 
A surface was created around the circumference of the shaft where the coupling is connected, on which an external torque can be applied, as shown in Figure~\ref{fig:Shaft_surface}. In practice, the torque is mainly transmitted through the keyway, with only a fraction carried by friction between coupling and shaft. However, since the objective is to evaluate the stress flow through the shoulder radius, the full torque is applied as a distributed load around the circumference. This represents a simplification that is justified as the only concern is the torque flow through the radius region.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{shaftsurface-Photoroom}
    \caption{Shaft with surface on which an external torque will be applied.}
    \label{fig:Shaft_surface}
\end{figure}
 
The shaft was fixed at the section where torque is transmitted to the robot link, as shown in Figure~\ref{fig:Shaft_fixed}. Although the two keyways are the actual torque--transmitting features, the objective of the analysis is to evaluate the stress flow through the fillet radius rather than the keyway stresses. Therefore, the fixed boundary condition was applied to the shaft circumference. Since the boundary condition is located sufficiently far from the abutment fillet, its influence on the fillet stress distribution is negligible.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{shaftfixed-Photoroom}
    \caption{Shaft fixed at the section where torque is transmitted to the robot link.}
    \label{fig:Shaft_fixed}
\end{figure} 

An external torque of \SI{35}{\newton\metre} is applied to this surface, as shown in Figure~\ref{fig:Shaft_torque}. As calculated in Section~\ref{subsec:key_verification}, the upgraded key material allows a maximum transmissible torque of \SI{34.5}{\newton\metre}.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{35nmshaft}
    \caption{\SI{35}{\newton\metre} applied to the shaft surface.}
    \label{fig:Shaft_torque}
\end{figure}
  
\subsubsection{Results}

The resulting von Mises stress distribution is shown in Figure~\ref{fig:shaft_vm}. The maximum stress of \SI{1575}{\mega\pascal} occurs at the shoulder fillet radius, confirming that this is the critical location on the shaft.

\begin{equation}
    S_{\text{shoulder}} = \frac{R_{p0.2}}{\sigma_{\text{vM,max}}} = \frac{2050}{1575} = 1.30
    \label{eq:shaft_shoulder_sf}
\end{equation}

The safety factor of 1.30 at the shoulder radius is below the general design target of 1.5. However, this load case represents the absolute upper bound. The \SI{35}{\newton\metre} corresponds to the ultimate transmissible torque of the upgraded 42CrMo4 key connection as calculated in Section~\ref{subsec:key_verification}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{backgroundremoved_35nm}
    \caption{Von Mises stress distribution under \SI{35}{\newton\metre} torque. Maximum stress of \SI{1575}{\mega\pascal} at the shoulder fillet.}
    \label{fig:shaft_vm}
\end{figure}

The keyway stress concentration is again verified at this upper--bound torque:

\begin{equation}
    W_p = \frac{\pi \cdot d^3}{16} = \frac{\pi \cdot 8^3}{16} = \SI{100.5}{\milli\metre\cubed}
    \label{eq:polar_modulus}
\end{equation}

\begin{equation}
    \tau_{\text{nom}} = \frac{T}{W_p} = \frac{35\,000}{100.5} = \SI{348}{\mega\pascal}
    \label{eq:torsion_stress}
\end{equation}

\begin{equation}
    \tau_{\text{eff}} = K_{ts} \cdot \tau_{\text{nom}} = 3.0 \cdot 348 = \SI{1045}{\mega\pascal}
    \label{eq:torsion_stress_eff}
\end{equation}

\begin{equation}
    \tau_{\text{allow}} = \frac{R_{p0.2}}{\sqrt{3}} = \frac{2050}{1.732} = \SI{1183}{\mega\pascal}
    \label{eq:torsion_allow}
\end{equation}

\begin{equation}
    S_{\text{torsion}} = \frac{\tau_{\text{allow}}}{\tau_{\text{eff}}} = \frac{1183}{1045} = 1.13
    \label{eq:torsion_sf}
\end{equation}

The torsion safety factor of 1.13 at the keyway is also below the 1.5 target. The same justification applies: this load level can only be reached with the upgraded 42CrMo4 key installed, and even then represents the key connection's ultimate capacity rather than a design service load. The shaft geometry is therefore adequate for all realistic operating conditions.

\subsection{Design and Analysis of the Joint Fixtures}
\label{subsec:joint_fixtures}

Since the transmission is rigidly coupled to the joint shaft, precise alignment between the two is critical to ensure free rotation without introducing parasitic forces. The joint shaft is supported by two bearings so that the transmission supplies only torque, while the bearings carry all radial and axial loads. To maintain proper shaft alignment, the transmission is bolted directly to the joint fixture and located by a centering feature, ensuring concentricity between the transmission output and the bearing bores. An overview of the shoulder and elbow joint fixtures is shown in Figures~\ref{fig:shoulder_fixture_dimensions} and~\ref{fig:elbow_fixture_dimensions}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{ShoulderWithLengths}
    \caption{Shoulder fixture with dimensions.}
    \label{fig:shoulder_fixture_dimensions}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Elbowfixturedimensions}
    \caption{Elbow fixture with dimensions.}
    \label{fig:elbow_fixture_dimensions}
\end{figure}

The geometry of the aluminium fixtures is primarily dictated by the dimensions of the transmission. Bearings were selected based on the shaft diameter, with the smallest suitable variant chosen to minimise the required axial space, thereby maximising the volume of material that can be removed and reducing the overall weight of the fixtures. Wall thicknesses in the bearing seats are determined by the SKF catalogue guidelines, and the groove geometry for the retaining circlips follows DIN~472. A custom-designed bearing retainer plate, shown in blue in Figure~\ref{fig:Crosssection}, constrains the bearing axially and accommodates a spring washer to handle residual axial forces, dimensioned following the guidelines of the SKF catalogue.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Crosssection}
    \caption{Cross-section of the shoulder fixture (green). From left to right: the KBK KBST2 size~8 coupling, the custom bearing retainer plate (blue), and the two bearings.}
    \label{fig:Crosssection}
\end{figure}

The shoulder and elbow fixtures share the same geometry; the elbow fixture is a slightly scaled-down variant to accommodate the smaller transmission used at that joint. The selected material is aluminium EN~AW~6061-T6, chosen for its favourable strength-to-weight ratio and machinability. The shoulder fixture was machined from a stock block of $97 \times 139.5 \times \SI{100}{\milli\metre}$, with a raw mass of \SI{3653.5}{\gram}. The final fixture has a mass of \SI{316.2}{\gram}, representing a material reduction of \SI{91.3}{\percent}. The elbow fixture was machined from a stock block of $71 \times 129.5 \times \SI{72}{\milli\metre}$, with a raw mass of \SI{1787.4}{\gram}. The final fixture has a mass of \SI{232.4}{\gram}, representing a material reduction of \SI{87.0}{\percent}.

\newpage

\subsubsection{Weight Reduction}

The structural integrity of the fixtures was verified using FEA. The selected material, EN~AW~6061-T6, has a yield strength $R_{p0.2} = \SI{276}{\mega\pascal}$ and a tensile strength $R_m = \SI{310}{\mega\pascal}$. Since the shoulder and elbow fixtures share the same geometry, only the analysis of the shoulder fixture is presented below; the same method applies to the elbow fixture. The final shape was arrived at through a combination of a topolgy study with a minimum static safety factor of 1.25 and longitudinal symmetry, iterative FEA, and design--for--manufacture considerations. For example, fillet radii are unified across the part to allow a single tool pass, and flat clamping surfaces are preserved for CNC fixturing.

Given that the fixture geometry is largely governed by bearing seat and circlip groove requirements rather than structural demand, the resulting stress levels remain well within the material's yield strength under both loading cases. The primary objective of the FEA was therefore not to verify structural integrity but to guide weight reduction: material was iteratively removed from non--critical regions while preserving the bearing seat walls, circlip groove dimensions, and the bearing retainer plate interface.

\paragraph{Load case 1: Bearing loads.}
A downward force was applied to the bearing surfaces to simulate the static loading imposed by the arm and payload. The total weight of the robotic arm including payload is estimated at approximately \SI{2.5}{\kilogram}, corresponding to approximately \SI{25}{\newton}. A load of \SI{60}{\newton} was applied to the bearing surfaces as shown in Figure~\ref{fig:Applied_forces}, providing a substantial safety margin.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{ShoulderForces}
    \caption{\SI{60}{\newton} applied downward on the bearing surfaces.}
    \label{fig:Applied_forces}
\end{figure}

Initially, the topology result indicated that a vertical column of material aligned with the applied force would be sufficient. However, in practice the arm is also subject to horizontal forces, and a purely vertical structure would be insufficient in that direction. A forked structure was therefore adopted, extending from the bearing seats outward to the sides of the fixture. This design choice was subsequently evaluated through a topology study, as shown in Figure~\ref{fig:fea_forces}. Although the result suggests that further material removal is possible, this does not account for manufacturability constraints such as the tangential radii connecting the legs and the material required around the through--holes next to the bearing seats.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{FeaForces}
    \caption{Topology result for the downward bearing load case.}
    \label{fig:fea_forces}
\end{figure}
\newpage
\paragraph{Load case 2: Torque.}
A torque was applied to simulate the load path through the transmission interface, and the fixture was constrained at its mounting plane, consistent with the physical boundary conditions. Since the transmission is bolted to the fixture, torque is transmitted through friction across the annular contact face between the two components. A torque of \SI{35}{\newton\metre} was applied to this surface, corresponding to the maximum allowable torque with the stronger 1.7225 key as calculated in Section~\ref{subsec:referToThisFea}. The applied torque is shown in Figure~\ref{fig:Applied_torque}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{appliedtorqueshoulder}
    \caption{Annular friction surface on the shoulder fixture to which the torque is applied. Note that in this figure material has already been removed.}
    \label{fig:Applied_torque}
\end{figure}

The topology result, shown in Figure~\ref{fig:fea_torque}, indicates that material around the centering feature can be largely removed. This result is physically unrealisable as it would leave the centering feature unsupported. A support structure was added with manufacturability constraints in mind.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{feaShoulder}
    \caption{Topology result for the torque load case.}
    \label{fig:fea_torque}
\end{figure}

\newpage

\paragraph{FEA verification.}

After the weight--reduced geometry was finalised based on the topology results and manufacturability constraints, a static FEA was performed on the final shape to verify the stress and displacement levels under both load cases.

Under the \SI{60}{\newton} bearing load (Figure~\ref{fig:Applied_forces}), the maximum von Mises stress is \SI{6.08e-3}{\mega\pascal} and the maximum displacement is \SI{9.05e-6}{\milli\metre}, as shown in Figures~\ref{fig:fixture_vm_force} and~\ref{fig:fixture_ures_force} respectively. Under the \SI{35}{\newton\metre} torque (Figure~\ref{fig:Applied_torque}), the maximum von Mises stress is \SI{9.73}{\mega\pascal} and the maximum displacement is \SI{9.92e-3}{\milli\metre}, as shown in Figures~\ref{fig:fixture_vm_torque} and~\ref{fig:fixture_ures_torque} respectively.

In both cases the stresses are negligible compared to the yield strength of \SI{276}{\mega\pascal}, confirming that the fixture geometry is vastly overdesigned from a structural standpoint. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{VonMisesFEA_Fixture}
    \caption{Von Mises stress distribution under \SI{60}{\newton} bearing load. Maximum stress: \SI{6.08e-3}{\mega\pascal}.}
    \label{fig:fixture_vm_force}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{uresFixture}
    \caption{Displacement under \SI{60}{\newton} bearing load. Maximum displacement: \SI{9.05e-6}{\milli\metre}.}
    \label{fig:fixture_ures_force}
\end{figure}
\newpage

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{TorqueVM}
    \caption{Von Mises stress distribution under \SI{35}{\newton\metre} torque. Maximum stress: \SI{9.73}{\mega\pascal}.}
    \label{fig:fixture_vm_torque}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Torque_mm}
    \caption{Displacement under \SI{35}{\newton\metre} torque. Maximum displacement: \SI{9.92e-3}{\milli\metre}.}
    \label{fig:fixture_ures_torque}
\end{figure}
\newpage

\subsection{Base Structure}
\label{subsec:base_structure}
The base structure serves as the stationary foundation of the testbench and carries the passive joint. It was fabricated entirely from \SI{2}{\milli\metre} thick stainless steel AISI~304 sheet metal. This material was selected because of its high strength and stiffness even at low sheet thickness, it is readily available in standard sheet formats, and it is well suited to conventional sheet metal fabrication processes such as laser cutting and bending. Additionally, stainless steel's high density is advantageous here, unlike the moving links of the robot, the base benefits from a high mass as it must keep the testbench stationary during operation. No effort was therefore made to reduce the weight of the base. The overall dimensions of the base are shown in Figure~\ref{fig:BaseDimensions}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{BaseWithDimensionsAndCoordsystem}
    \caption{Stainless steel base construction with overall dimensions.}
    \label{fig:BaseDimensions}
\end{figure}

The primary design constraints were manufacturability and ease of assembly rather than structural optimisation. At \SI{2}{\milli\metre} thickness, AISI~304 provides more than sufficient strength for a static support structure, so the geometry was driven by practical fabrication and assembly considerations. A cross section of the base is shown in Figure~\ref{fig:RedCross}. The base consists of a top half and a bottom half, connected by a central cross-shaped bracket highlighted in red. Four identical side support panels stiffen the assembly against lateral forces in the $y$-direction. The gaps between the side panels provide hand access for bolting the assembly together and for reaching the passive joint.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{RedCross}
    \caption{Cross section of the base structure. The red cross--shaped bracket connects the top and bottom halves.}
    \label{fig:RedCross}
\end{figure}

Several standard sheet metal design rules were adhered to throughout the base design. Holes were placed outside the minimum leg length zone of the bend, which for \SI{2}{\milli\metre} stainless steel at \SI{90}{\degree} corresponds to approximately \SI{9.2}{\milli\metre} from the bend line. This prevents hole deformation caused by material flow inside the V--block of the press brake during bending. The spacing between consecutive bends was kept larger than the rib height so that already--bent flanges would not collide with the press brake tooling during subsequent bending operations.

Figure~\ref{fig:TransparentViewBase} shows a view from the $zy$-plane, where the front panels have been made transparent to show the internal structure. The passive joint is mounted to the base with four bent brackets, two on each side.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{RedBrackets}
    \caption{View from the $zy$-plane. Bent brackets, shown in red, support the passive joint.}
    \label{fig:TransparentViewBase}
\end{figure}
\newpage

A deliberate effort was made to maximise the use of identical parts throughout the base assembly. This served a dual purpose; it eliminated the possibility of orientation or placement errors during the bending process, and it simplified assembly since parts could not be installed in the wrong position. The geometry also provides internal clearance, allowing hand access for bolting components together.

All bolted connections use M3 through--bolts with nuts, and four mounting holes are provided in the bottom panel, allowing the base to be bolted to a heavy plate or workbench for additional stability, as shown in Figure~\ref{fig:bottomHoles}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{bottomHoles}
    \caption{Bottom view of base structure, showing the four holes available for mounting the base to a heavy plate or workbench.}
    \label{fig:bottomHoles}
\end{figure}

\subsection{Passive Joint}
The passive joint provides rotation about the vertical $z$-axis and is mounted to the top of the base structure by means of four bent stainless steel brackets, as described in Section~\ref{subsec:base_structure}. It is referred to as passive because, unlike the shoulder and elbow joints, it has no actuation unit. When the operator rotates the arm about the $z$--axis, the only resistance encountered is the inertia of the rotating assembly. This is analogous to the vertical direction, where weight compensation eliminates gravitational load so that the operator only feels inertia. Active actuation of this joint is therefore not required for the intended assisted load handling task. The mechanical design nevertheless reserves space for a future transmission and motor, so that autonomous movement or impedance control can be implemented at a later stage without redesigning the base structure.
 
The same flanged shaft coupling used at the shoulder and elbow links is used at the passive joint. This coupling connects the vertical shaft to the sheet metal part that links the passive joint to the shoulder fixture. A cross--section of the passive joint assembly is shown in Figure~\ref{fig:Cross_sectionPassivejoint}.
\newpage

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{Schermafbeelding 2026-05-05 104649}
    \caption{Cross--section of the passive joint. The purple volume represents a mock transmission included to verify that sufficient space remains for future implementation. The blue part is a custom bearing retainer plate that constrains the bearing axially. The conical shaft with conical adapter ring supports the weight of the robotic arm. The flanged coupling (black) is bolted to the mounting plate on which the shoulder fixture sits, and is connected to the shaft by a parallel key. Two set screws prevent accidental upward displacement of the flanged coupling.}
    \label{fig:Cross_sectionPassivejoint}
\end{figure}
 
Unlike the actuated joints, where a longer shaft directly increases the moment arm of the transmission and motor mass, the passive joint has no such constraint. The additional axial space was used to design a shaft with a conical transition rather than a sharp step. The lower portion of the shaft has a nominal diameter of \SI{8}{\milli\metre}, matching the flanged coupling bore. This diameter transitions through a conical section to \SI{25}{\milli\metre}, where a standard conical adapter ring with a matching taper angle provides a flat seating surface for the flanged coupling above it. The shaft then steps to a diameter of \SI{30}{\milli\metre}, which forms the abutment shoulder for the bearings. The conical geometry increases the shaft cross--section in the region of highest bending stress and provides a gradual transition that is inherently more favourable than a right--angle shoulder with a fillet, where the abrupt change in cross--section introduces a higher stress concentration. The shaft abutment radii and tolerances were again designed following SKF catalogue guidelines, as was done for the actuated joints.
 
The shaft is supported by two deep groove ball bearings arranged vertically. The centre--to--centre distance between the bearings is \SI{37}{\milli\metre}, composed of a \SI{25}{\milli\metre} spacer between the inner rings and one half--width of each bearing (\SI{6}{\milli\metre} each).
 
A FEA was used to verify the shaft design. The two bearing surfaces were fixed and the worst--case loading was applied, corresponding to the fully stretched arm configuration. The gravitational loads were multiplied by a safety factor of~2 to conservatively account for dynamic effects during operation. The applied loads were \SI{24}{\newton} at \SI{0,2}{\metre} and \SI{20}{\newton} at \SI{0,4}{\metre}. The loading configuration and dimensions are shown in Figure~\ref{fig:shaftfeawithforces}.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{shaftFea.drawio}
    \caption{Loading configuration and dimensions for the passive joint shaft FEA. Fixed supports are applied at the bearing surfaces.}
    \label{fig:shaftfeawithforces}
\end{figure}
 
The resulting peak von~Mises stress was \SI{84,5}{\mega\pascal}, located at the transition between the cylindrical and conical shaft sections, as shown in Figure~\ref{fig:passive_joint_fea}. Compared to the yield strength of \SI{2050}{\mega\pascal}, this corresponds to a safety factor of approximately~24.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{FeaResults}
    \caption{Von~Mises stress distribution on the passive joint shaft under conservative loading (\SI{24}{\newton} at \SI{0,2}{\metre} and \SI{20}{\newton} at \SI{0,4}{\metre}). Peak stress: \SI{84,5}{\mega\pascal}.}
    \label{fig:passive_joint_fea}
\end{figure}
\newpage

The large safety margin confirms that the shaft is well within its structural limits for the applied loading.
 
Regarding the bearing spacing, the moment from the arm and payload is loaded on the two SKF 6005--2Z bearings. Given the low mass of the arm assembly and the high static load rating of the 6005--2Z ($C_0 = \SI{6550}{\newton}$), the resulting radial loads are a small fraction of the bearing capacity at any reasonable spacing. The centre--to--centre distance of \SI{37}{\milli\metre} was therefore determined by the available axial space within the base structure and manufacturability considerations rather than by a bearing load optimisation.

As shown in Figure~\ref{fig:ballrolers}, six ball rollers are mounted on the upper half of the base structure. The rotating platform rests on these ball rollers, which carry the full axial load from the weight of the arm assembly. The four bent brackets are then tightened to fix the passive joint assembly in the $xy$--plane, preventing lateral displacement. This also simplifies assembly; the platform is placed onto the ball rollers and can be positioned before the brackets are tightened. The ball rollers are spaced by 3D--printed spacers, allowing the vertical clearance to be adjusted. Implementation required only additional holes in the sheet metal and inexpensive off--the--shelf components.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{ballrolers}
    \caption{Top--down view of the base structure showing the six ball rollers that support the rotating platform.}
    \label{fig:ballrolers}
\end{figure}
\newpage

\subsection{Aluminium Link Structures}

The aluminium link structures connect the joint fixtures and together form the robotic arm. The links are fabricated from EN~AW--5754 (AlMg3) sheet metal, selected for its good formability. Each link consists of two nested U--shaped profiles; a larger outer profile and a smaller inner profile with ribs, bolted together through matching holes to form a closed box. A bolted assembly was chosen over welding to simplify assembly and avoid fabrication risks. Welding thin aluminium sheet is difficult due to its high thermal conductivity and low melting point, which make burn--through likely; a welded construction would therefore have required stainless steel, significantly increasing the mass. As with the base structure, the link design follows the same standardised parts and manufacturability constraints outlined in Section~\ref{subsec:base_structure}. Practical considerations such as mounting points and routing space for cables inside the link were integrated into the design. The upper arm link is shown in green and the forearm link in red in Figure~\ref{fig:NumbersOverview}. To reduce mass, a topology optimisation study was performed on both links and the strength of the final design was validated with a FEA.

\subsubsection{Upper Arm Link}

The upper arm link connects the shoulder joint to the elbow joint. The link before topology optimisation is shown in Figure~\ref{fig:SideOnderSL}. The link consists of three bent sheet metal parts, therefore a single--body model was created in SolidWorks by merging the assembly into one part to perform the topology study.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{SideOnderSL}
    \caption{Single--body model of the upper arm link used for the topology study, combining the two nested U--profiles and the end flange into one part.}
    \label{fig:SideOnderSL}
\end{figure}

The topology study was set up on the single--body model, which has a mass of \SI{209,42}{\gram} before optimisation. Forces were applied to represent the weights of the transmission--motor assembly, the elbow joint fixture, the elbow joint shaft with flanged couplings, and the payload at the end--effector. A downward force of \SI{15}{\newton} was applied at the elbow side with an offset to account for the centre of gravity of the transmission--motor assembly, which introduces a torque in addition to the gravitational force. At the end--effector side, a force of \SI{25}{\newton} was applied. The estimated static load at this point is approximately \SI{11}{\newton}, composed of the \SI{1}{\kilo\gram} payload and the mass of the forearm link. The applied value of \SI{25}{\newton} includes a safety factor of approximately~2, consistent with the approach used for the passive joint shaft verification. This margin also ensures that the link remains structurally adequate should the payload capacity be increased in the future, for instance by upgrading to the stronger 1.7225 key documented in Section~\ref{subsec:referToThisFea}. The structure was fixed at the four bolt holes on either side, where the flanged couplings connect the link to the joint shafts. The loading configuration is shown in Figures~\ref{fig:UnderarmSideforce} and~\ref{fig:Underarm_eeforce}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{UnderarmSideforce}
    \caption{Side view of the upper arm link showing the applied forces for the topology study.}
    \label{fig:UnderarmSideforce}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{Underarm_eeforce}
    \caption{End--effector side of the upper arm link showing the applied payload force.}
    \label{fig:Underarm_eeforce}
\end{figure}

The resulting topology optimisation reduced the link mass to approximately \SI{147}{\gram}, a reduction of \SI{30}{\percent}. The topology result was then applied to the link geometry by removing material from the corresponding regions. The topology result and the design with applied material removal are compared side by side in the following figures. For each view, the topology result is shown on the left and the applied material removal on the right.
\newpage

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{FLTOPtop}
        \caption{Top view upper arm: topology result.}
        \label{fig:FLTOPtop}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{sl_topview}
        \caption{Top view upper arm: applied material removal.}
        \label{fig:sl_topview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{FLSIDETOP}
        \caption{Side view upper arm: topology result.}
        \label{fig:FLSIDETOP}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{sl_othersideview}
        \caption{Side view upper arm: applied material removal.}
        \label{fig:sl_othersideview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{FLSIDETOP2}
        \caption{Opposite side upper arm: topology result.}
        \label{fig:FLSIDETOP2}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{fl_sideview}
        \caption{Opposite side upper arm: applied material removal.}
        \label{fig:fl_sideview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{UndersideFLTop}
        \caption{Underside upper arm: topology result.}
        \label{fig:UndersideFLTop}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{fl_bottomview}
        \caption{Underside upper arm: applied material removal.}
        \label{fig:fl_bottomview}
    \end{minipage}
\end{figure}

The design with applied material removal was validated with a static FEA using the same forces and boundary conditions as the topology study. The von~Mises stress distribution is shown in Figures~\ref{fig:cutout_sideview_otherside} through~\ref{fig:Cutout_VM_sideview}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{cutout_sideview_otherside}
    \caption{Von~Mises stress distribution upper arm: side view.}
    \label{fig:cutout_sideview_otherside}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{CutoutVM_SIDETOP}
    \caption{Von~Mises stress distribution upper arm: top view.}
    \label{fig:CutoutVM_SIDETOP}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Cutout_VM_sideview}
    \caption{Von~Mises stress distribution upper arm: opposite side view.}
    \label{fig:Cutout_VM_sideview}
\end{figure}

The peak von~Mises stress is \SI{22,9}{\mega\pascal}, located at the edge of one of the fixture holes where the boundary condition is applied. In reality, the load transfer at these holes occurs through bolt clamping and friction over the flange surface rather than through the hole edges alone, so this local stress concentration is an artifact of the fixed boundary condition. Away from the fixture holes, the stresses are significantly lower. Compared to the yield strength of EN~AW--5754 ($R_{p0,2} \geq \SI{80}{\mega\pascal}$), the peak stress gives a safety factor of at least~3,5, even including the boundary condition artifact. The displacement under the applied loading is shown in Figure~\ref{fig:sideview_uresmm}. The maximum displacement is \SI{9,4e-2}{\milli\metre}, which is negligible relative to the link dimensions.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{sideview_uresmm}
    \caption{Displacement distribution of the upper arm link under the applied loading. Maximum displacement: \SI{9,4e-2}{\milli\metre}.}
    \label{fig:sideview_uresmm}
\end{figure}

The final manufactured design, incorporating the topology--guided material removal with adjustments for bend zone clearances, is shown in Figures~\ref{fig:actualmanufactured_sidetop} through~\ref{fig:actual_manufactured_bttomu}. The flange plate that connects the link to the joint fixture is shown in Figure~\ref{fig:frontplate}.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{actualmanufactured_sidetop}
        \caption{Final design upper arm: isometric view.}
        \label{fig:actualmanufactured_sidetop}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{actualmanufactured_topview}
        \caption{Final design upper arm: top view.}
        \label{fig:actualmanufactured_topview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{actualManufacturedPart_sideview}
        \caption{Final design upper arm: opposite side view.}
        \label{fig:actualManufacturedPart_sideview}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{actualmanufactured_othersideview}
        \caption{Final design upper arm: side view.}
        \label{fig:actualmanufactured_othersideview}
    \end{minipage}
\end{figure}
\newpage

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{actual_manufactured_bttomu}
        \caption{Final design upper arm: inner U--profile.}
        \label{fig:actual_manufactured_bttomu}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{frontplate}
        \caption{Flange plate connecting the upper arm link to the joint fixture.}
        \label{fig:frontplate}
    \end{minipage}
\end{figure}

\subsubsection{Forearm Link}

The forearm link connects the elbow joint to the end--effector. An identical design and analysis approach was followed as for the upper arm link. The structure was fixed at the four bolt holes and a downward force of \SI{25}{\newton} was applied at the end--effector, consistent with the loading used for the upper arm link. The mass of the forearm link before topology optimisation is \SI{155}{\gram}. The closed single--body model used for the topology study is shown in Figure~\ref{fig:Forearmfortopology}, and the applied forces are shown in Figure~\ref{fig:forearm_appliedforces}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Forearmfortopology}
    \caption{Single--body model of the forearm link used for the topology study.}
    \label{fig:Forearmfortopology}
\end{figure}
\newpage

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{forearm_appliedforces}
    \caption{Applied force for the forearm link topology study.}
    \label{fig:forearm_appliedforces}
\end{figure}

The topology result and the design with applied material removal are compared side by side in the following figures.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearmTop_sideview}
        \caption{Side view forearm: topology result.}
        \label{fig:forearmTop_sideview}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearmMaterialremoved_sideview}
        \caption{Side view forearm: applied material removal.}
        \label{fig:forearmMaterialremoved_sideview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearm_top_topview}
        \caption{Top view forearm: topology result.}
        \label{fig:forearm_top_topview}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearmtopviewcutout}
        \caption{Top view forearm: applied material removal.}
        \label{fig:forearmtopviewcutout}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearm_undersideToplogy}
        \caption{Underside forearm: topology result.}
        \label{fig:forearm_undersideToplogy}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{forearm_materialremoved_under}
        \caption{Underside forearm: applied material removal.}
        \label{fig:forearm_materialremoved_under}
    \end{minipage}
\end{figure}

In addition to the vertical loading, a horizontal force of \SI{25}{\newton} was applied at the end--effector mounting point for the FEA validation, as shown in Figure~\ref{fig:torqueforearm}. This lateral load simulates the force exerted by the operator when rotating the arm about the passive joint axis or when pushing the payload sideways. To verify that this value is conservative, the horizontal force required to accelerate the fully stretched arm about the $z$--axis can be estimated. With a moment of inertia of $J_z = \SI{1,2}{\kilo\gram} \cdot \SI{0,2}{\metre}^2 + \SI{1,0}{\kilo\gram} \cdot \SI{0,4}{\metre}^2 = \SI{0,208}{\kilo\gram\metre\squared}$ and an angular acceleration of $\alpha = \SI{4,9}{\radian\per\second\squared}$ (corresponding to a $90^\circ$ sweep in \SI{0,8}{\second}), the required torque is $T_z = J_z \cdot \alpha = \SI{1,0}{\newton\metre}$. At the end--effector (\SI{0,4}{\metre} from the $z$--axis), this corresponds to a horizontal force of only \SI{2,6}{\newton}. The applied \SI{25}{\newton} therefore exceeds the expected operational loading by approximately a factor of ten.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{torqueforearm}
    \caption{Horizontal force applied at the end--effector mounting point.}
    \label{fig:torqueforearm}
\end{figure}

The von~Mises stress distribution from the FEA validation is shown in Figure~\ref{fig:forearmvonmiseundersideview}. The peak von~Mises stress is \SI{38,3}{\mega\pascal}. Compared to the yield strength of EN~AW--5754 ($R_{p0,2} \geq \SI{80}{\mega\pascal}$), this gives a safety factor of at least~2,1. Given that the applied horizontal load is approximately ten times the expected operational force, the actual safety margin during normal operation is substantially higher.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{forearmvonmiseundersideview}
    \caption{Von~Mises stress distribution of the forearm link.}
    \label{fig:forearmvonmiseundersideview}
\end{figure}

\newpage

The displacement under the applied loading is shown in Figures~\ref{fig:Forearmuresmm} and~\ref{fig:forearmuresfrontview}. The maximum displacement is \SI{3,1e-1}{\milli\metre}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Forearmuresmm}
    \caption{Side view of displacement of the forearm link. Maximum displacement: \SI{3,1e-1}{\milli\metre}.}
    \label{fig:Forearmuresmm}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{forearmuresfrontview}
    \caption{Front view of displacement of the forearm link.}
    \label{fig:forearmuresfrontview}
\end{figure}

The applied material removal was then adjusted to account for manufacturing constraints. A key constraint is that material cannot be removed within the bend zone of the sheet metal. If cutouts extend into the V--block, the part will deforms incorrectly during bending, as illustrated in Figures~\ref{fig:FailedTopview} and~\ref{fig:FailedSideView}. The correctly bent part with cutouts placed outside the bend zone is shown in Figure~\ref{fig:CorrectTopview}. This constraint limits the amount of material that can be removed compared to the topology result and requires adding back material in the bend regions.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{FailedTopview}
    \caption{Top view of an incorrectly manufactured part: cutouts extending into the bend zone cause deformation during bending.}
    \label{fig:FailedTopview}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{FailedSideView}
    \caption{Side view of the same incorrectly manufactured part, showing the deformed bend line.}
    \label{fig:FailedSideView}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{CorrectTopview}
    \caption{Correctly manufactured part with cutouts placed outside the bend zone.}
    \label{fig:CorrectTopview}
\end{figure}

The final manufactured forearm link design is shown in Figures~\ref{fig:manufacturedforearm_sideview} through~\ref{fig:manufactured_forearm_inneru}. The final manufactured mass is \SI{130}{\gram}, a reduction of \SI{16}{\percent} compared to the original \SI{155}{\gram}.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{manufacturedforearm_sideview}
        \caption{Final forearm design: side view.}
        \label{fig:manufacturedforearm_sideview}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{manufacturedforearm_topview}
        \caption{Final forearm design: top view.}
        \label{fig:manufacturedforearm_topview}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{manufactured_forearm_inneru}
    \caption{Final forearm design: inner U--profile.}
    \label{fig:manufactured_forearm_inneru}
\end{figure}
\newpage

\subsection{Joint End Stop}

When the upper arm and forearm become collinear, the elbow approaches a kinematic singularity. For a planar two--link manipulator, the end--effector velocity is related to the joint velocities through the Jacobian matrix

\begin{equation}
    \dot{\bm{x}} = \bm{J}(\bm{q}) \dot{\bm{q}}
\end{equation}

For this configuration, the determinant of the Jacobian is

\begin{equation}
    \det(\bm{J}) = l_1 l_2 \sin(q_2)
\end{equation}

where $q_2$ is the elbow angle and $l_1$, $l_2$ are the link lengths. As the arm approaches the fully extended configuration, $\sin(q_2)$ approaches zero, causing the Jacobian to lose rank. This means that the columns of the Jacobian become linearly dependent: different joint motions produce motion in the same end--effector direction instead of independent directions. The arm therefore loses one degree of controllable motion and can no longer generate or resist forces in the lost direction. In this case, the elbow loses its ability to resist forces acting along the line of the arm.

To prevent operation near this singular configuration, a mechanical end stop is installed at the elbow joint. The end stop consists of a shock absorber mounted to the upper arm and an adjustable contact plate mounted to the forearm. The contact plate position can be tuned using slotted mounting holes, allowing the engagement point to be set before full extension is reached.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{endstop_sideview}
    \caption{Side view of the mechanical end stop at the elbow joint.}
    \label{fig:endstop_sideview}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{endstop_topview}
    \caption{Top view of the mechanical end stop showing the slotted mounting holes for position adjustment.}
    \label{fig:endstop_topview}
\end{figure}

Rather than producing a rigid impact, the shock absorber provides force feedback over a stroke of \SI{10}{\milli\metre}. This reduces impact loads on the mechanism and provides the operator with a gradual increase in resisting force as the arm approaches its motion limit. The compliant stop gives the operator time to react before the hard limit is reached, resulting in safer operation.
\newpage

\subsection{End--Effector}

The end--effector consists of a cylindrical pen supported by two flanged linear bearings, providing free axial sliding with minimal friction. Loads can be attached by sliding the pen out, mounting the payload, and reinserting the pen. Alternatively, a platform can be mounted on the pen onto which weights can be stacked, as shown in Figure~\ref{fig:End_effector_overview}. Two 3D--printed guides  create a \SI{4}{\milli\metre} gap to limit sway of the payload. A cross--section of the end--effector assembly is shown in Figure~\ref{fig:Endeffector_cross_section}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{End_effector_overview}
    \caption{Overview of the end--effector with the weight platform mounted.}
    \label{fig:End_effector_overview}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Endeffector_cross_section}
    \caption{Cross--section of the end--effector showing the pen, flanged linear bearings, and 3D--printed guides.}
    \label{fig:Endeffector_cross_section}
\end{figure}

\section{Sensors and Data communication}

\subsection{Communication Architecture}
\label{sec:communication-architecture}

The proposed controller runs on the Jetson Orin~NX at a rate of \SI{1}{\kilo\hertz}; the motivation for this choice is discussed in Chapter~\ref{ch:control}. The Jetson Orin~NX was selected because it provides a native CAN interface, avoiding the need for an external USB-to-CAN adapter.

Three motor drivers were considered: the ESCON, the ESCON2, and the EPOS4. The ESCON uses block commutation, which produces a torque ripple of approximately \SI{14}{\percent}. In contrast, the ESCON2 employs field--oriented control (FOC) with sinusoidal commutation, significantly reducing torque ripple. Since the application is hand--guided and collaborative, the operator directly perceives the motor torque through the arm, making smooth torque generation desirable. Although the EPOS4 additionally supports position control modes, this functionality is not required because the Jetson controller commands torque. The ESCON2 Compact 60/12 was therefore selected, as it provides CANopen--based torque control with FOC commutation ~\cite{EPOS4ESCON2ESCON2025}.

All communication is carried out over a single CAN bus operating at \SI{1}{\mega\bit\per\second}, following the CANopen protocol stack (CiA~301) with the CiA~402 drive profile. The Jetson acts as the CANopen master. Three slave nodes are present on the bus:

\begin{enumerate}[noitemsep]
    \item ESCON2 Compact 60/12 shoulder joint: Maxon servo controller operating in Cyclic Synchronous Torque (CST) mode. It receives a torque set-point from the master and returns the motor encoder position.

    \item ESCON2 Compact 60/12 elbow joint: identical role and configuration as the shoulder controller.

    \item STM32G474RE sensor node: this microcontroller is the interface between the joint--side sensors and the CAN bus. The node reads two AS5048A magnetic rotary encoders over the Serial Peripheral Interface (SPI) to obtain the joint positions, and two spring--deflection potentiometers through an ADS1256 24--bit analog--to--digital converter (ADC), also connected via SPI. The four sensor measurements are packed into a single 8--byte CAN frame, corresponding to the maximum payload of a standard CAN frame.
\end{enumerate}

Figure~\ref{fig:can-topology} shows an example of a CAN bus topology, including the required \SI{120}{\ohm} termination resistors at both ends of the bus. In this schematic, the third node is replaced by the STM32 sensor node.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{esconschematic}
    \caption{Example of ESCON CAN bus topology with master and slave nodes. Termination resistors of \SI{120}{\ohm} are placed at both ends of the bus from~\cite{ESCON2CommunicationGuide}.}
    \label{fig:can-topology}
\end{figure}

The joint--side angular position is measured using two AS5048A contactless 14--bit absolute position sensors, as shown in Figure~\ref{fig:adapterboard}. The magnets are mounted on the joint shafts, while the sensors are mounted on the joint fixtures. With a resolution of 16384 positions per revolution, the sensor provides an angular resolution of approximately \SI{0.022}{\degree}. The AS5048A is specifically marketed for robotic joint position measurements. The sensor tolerates radial and axial misalignment, air-gap variations, and temperature variations over a range of \SI{-40}{\degreeCelsius} to \SI{150}{\degreeCelsius}, which simplifies mechanical integration ~\cite{AmsAS5048AHighResolution}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{AS5048A-AB-1.0_sml(200x200)}
    \caption{The AS5048A adapter board used for joint-side position 		sensing~\cite{AS5048ATS_EK_ABAmsOSRAM}.}
\label{fig:adapterboard}
\end{figure}

The spring deflection in each transmission is measured by a potentiometer whose output voltage is an analog signal digitised by an ADS1256 24--bit ADC. However, since potentiometers operate through sliding mechanical contact, they are subject to wear, noise, and drift caused by temperature variation and mechanical degradation. Their long--term suitability for this application has therefore not yet been validated. The AS5048A joint--side position sensors, combined with the motor encoder positions, provide an independent measurement of the spring deflection. This redundancy allows the potentiometer readings to be validated against the encoder--based measurements and makes it possible to characterise potentiometer degradation over time, including temperature effects and wear.

\newpage

\subsubsection{STM32 Sensor Node}
\label{sec:stm32-sensor-node}
 
The two joint--side position sensors and the ADC are connected to the STM32G474RE on a single SPI bus. The two AS5048A position sensors are connected via \SI{50}{\centi\metre} Cat~6 cables, while the Waveshare ADS1256 board is placed directly next to the STM32. The SPI clock frequency is set to \SI{1.33}{\mega\hertz}, resulting from a \SI{170}{\mega\hertz} peripheral clock with a prescaler of 128~\cite{GettingStartedSPI}. This remains within the \SI{1.92}{\mega\hertz} maximum of the ADS1256~(Appendix~\ref{app:ADS1256datasheet}).

Although the AS5048A specifies a maximum SPI clock frequency of up to \SI{10}{\mega\hertz} under a \SI{50}{\pico\farad} load condition (Appendix~\ref{app:AS5048AdatasheetCap}, Appendix~\ref{app:AS5048Adatasheet}), the \SI{50}{\centi\metre} Cat~6 cables introduce additional capacitive loading~\cite{Category6Cable}. This increases the effective load beyond the datasheet reference condition, motivating the use of a more conservative SPI clock frequency.

The STM32G474RE connects to the CAN bus via its integrated FDCAN peripheral, configured in Classical CAN mode to be compatible with the CANopen protocol used by the ESCON2 motor drivers. An external SN65HVD230 CAN transceiver provides the physical CAN bus interface.

\subsubsection{Communication schematic}

\subsubsection{Bus Timing}
\label{sec:bus-timing}

To verify that the \SI{1}{\kilo\hertz} control rate is achievable, the worst--case bus occupancy per cycle is calculated. A standard CAN data frame consists of the following fields, illustrated in Figure~\ref{fig:can-frame}: Start of Frame (1~bit), Arbitration 
Field (12~bits), Control Field (6~bits), Data Field (0 to 64~bits), Cyclic Redundancy Check Field (16~bits), Acknowledge Field (2~bits), End of Frame (7~bits), and Intermission field(3~bits). For a detailed description of each field: ESCON2 Communication Guide~\cite{ESCON2CommunicationGuide}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{can_frame}
    \caption{Standard CAN data frame 
    format~\cite{ESCON2CommunicationGuide}.}
    \label{fig:can-frame}
\end{figure}

The nominal frame length in bits is therefore:

\begin{equation}
    N_{\text{bits}} = 1 + 12 + 6 + 8 \cdot \text{DLC} + 16 + 2 + 7 
    + 3 = 47 + 8 \cdot \text{DLC}
    \label{eq:can-frame-bits}
\end{equation}

where DLC is the Data Length Code, specifying the number of data bytes in the CAN frame. CAN uses bit stuffing to maintain clock synchronisation between nodes on the bus. Since CAN nodes rely on signal transitions to keep their internal clocks synchronised, multiple bits with the same value could cause the nodes become desynchronised. To prevent this, the transmitter inserts a complementary bit after every five consecutive bits of the same value. In the worst case, this adds one extra bit for every five original bits, resulting in a \SI{20}{\percent} increase. This process is called bit stuffing. The worst--case duration of a frame at \SI{1}{\mega\bit\per\second} is :

\begin{equation}
    t_{\text{frame}} = \frac{N_{\text{bits}} \times 1{,}2}
    {\SI{1}{\mega\bit\per\second}}
    \label{eq:can-frame-time}
\end{equation}

All data frames are assumed to carry the maximum payload of 8~bytes (DLC~=~8), except for the SYNC frame which carries no data (DLC~=~0). Table~\ref{tab:bus-timing} shows the resulting frame durations, where the frame length is the amount of bytes before bit stuffing.

\begin{table}[htbp]
    \centering
    \begin{tabular}{l c c c}
        \toprule
        Frame & DLC [bytes] & Frame length [bits] & Worst-case duration with bit stuffing included \\
        \midrule
        SYNC                & 0 & 47  & $\approx \SI{56}{\micro\second}$ \\
        ESCON2 \#1 TPDO     & 8 & 111 & $\approx \SI{133}{\micro\second}$ \\
        ESCON2 \#2 TPDO     & 8 & 111 & $\approx \SI{133}{\micro\second}$ \\
        STM32 TPDO          & 8 & 111 & $\approx \SI{133}{\micro\second}$ \\
        ESCON2 \#1 RPDO     & 8 & 111 & $\approx \SI{133}{\micro\second}$ \\
        ESCON2 \#2 RPDO     & 8 & 111 & $\approx \SI{133}{\micro\second}$ \\
        \midrule
        \textbf{Total}      &   &     & $\approx \SI{721}{\micro\second}$ \\
        \bottomrule
    \end{tabular}
    \caption{Worst-case CAN bus timing per control cycle at 
    \SI{1}{\mega\bit\per\second}.}
    \label{tab:bus-timing}
\end{table}

Even in this worst case, the total bus occupancy is approximately \SI{721}{\micro\second}, or \SI{72}{\percent} of the \SI{1}{\milli\second} cycle period. 

When multiple nodes attempt to transmit after receiving SYNC, the frame with the lowest COB-ID is transmitted first. A Transmit Process Data Object (TPDO) is a CAN frame sent by a slave node to the master, while a Receive Process Data Object (RPDO) is a CAN frame sent by the master to a slave. Since the ESCON2 TPDOs are configured to have lower COB-IDs than the STM32 TPDO, the two motor driver frames are always transmitted first. This gives the STM32 approximately \SI{322}{\micro\second} (the SYNC frame plus two ESCON2 TPDOs) to complete its SPI sensor acquisition before its own TPDO can be sent on the CAN bus.

The AS5048A needs two 16--bit SPI transactions per read action. The first transmits the read command, and the sensor returns the requested data during the second transaction, as shown in Figure~\ref{fig:as5048a-spi-read}. Reading both AS5048A sensors 
therefore requires four 16--bit transactions, totalling 64~clock cycles. Between each transaction, three timing constraints must be respected. A minimum time $t_H = \SI{50}{\nano\second}$ from the last clock edge to the chip--select rising edge, a chip--select high time $T_{\text{CSnH}} = \SI{350}{\nano\second}$ between two transmissions, and a setup time $t_L = \SI{350}{\nano\second}$ from the chip--select falling edge to 
the first clock edge (Appendix~\ref{app:AS5048Adatasheet}). This results in a dead time of \SI{750}{\nano\second} per gap, or approximately \SI{2,25}{\micro\second} for the three gaps between 
four transactions.
\newpage

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{send_receiveAS}
    \caption{AS5048A SPI command/response 
    sequence~\cite{AmsAS5048AHighResolutiona}.}
    \label{fig:as5048a-spi-read}
\end{figure}

For the ADS1256, each channel read consists of an 8--bit RDATA command followed by 24~bits of data, totalling 32~clock cycles per channel. The ADS1256 additionally requires a delay $t_6$ of 50 master clock cycles between the command and the data output, as can be seen in Figure~\ref{fig:ads-timing}.

\begin{equation}
    t_6 = 50 \cdot \frac{1}{\SI{7,68}{\mega\hertz}} \approx 
    \SI{6,5}{\micro\second}
    \label{eq:t6-delay}
\end{equation}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{ads_timing}
    \caption{ADS1256 sequence~\cite{ADS1256DataSheet}.}
    \label{fig:ads-timing}
\end{figure}

The value for $t_6$ can be found in Appendix~\ref{app:ADS1256datasheet}. Since both potentiometer channels are read, this delay occurs twice, adding approximately \SI{13}{\micro\second} of waiting time to the total data acquisition.

The total SPI clock cycles is calculated:
\begin{equation}
    N_{\text{SPI}} = \underbrace{4 \cdot 16}_{\text{AS5048A}} + 
    \underbrace{2 \cdot (8 + 24)}_{\text{ADS1256}} = 128 
    \text{~clock cycles}
    \label{eq:spi-clocks}
\end{equation}

At \SI{1,33}{\mega\hertz} this corresponds to approximately \SI{96}{\micro\second}. The total sensor acquisition time including dead time is approximately \SI{110}{\micro\second}, which is well within 
the \SI{322}{\micro\second} available before the STM32 TPDO slot on 
the bus.
% ============================================================
%  BACK MATTER
% ============================================================
\backmatter

\printbibliography[heading=bibintoc, title={References}]

\appendix

\chapter{Maxon Motor Datasheet}
\label{app:MaxonMotorDatasheet}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{MaxonMotor}
    \caption{Maxon Brushless DC Motor \cite{BLDCmotorenBorstellozeElektromotoren}.}
    \label{fig:maxon_motor_datasheet}
\end{figure}

\chapter{ESCON2 Compact 60/12 Datasheet}
\label{app:ESCON2Datasheet}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{escon2p9}
    \caption{Technical data ESCON2 Compact 60/12 \cite{ESCON2Compact60}.}
    \label{fig:ESCON2_datasheetp9}
\end{figure}

\chapter{Peterson's stress concentration factors}
\label{app:peterson}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{pearsonKfactor}
    \caption{Peterson's stress concentration factors for a torsion shaft with a semicircular end keyseat\cite{doi:https://doi.org/10.1002/9780470211106.ch5}.}
    \label{fig:peterson}
\end{figure}


\chapter{ADS1256 Datasheet}
\label{app:ADS1256datasheet}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{ADS_timingdatasheet}
    \caption{ADS1256 datasheet\cite{ADS1256DataSheet}.}
    \label{fig:ADStiming}
\end{figure}

\chapter{AS5048A Datasheet}
\label{app:AS5048Adatasheet}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{AsTiming}
    \caption{AS5048A datasheet\cite{AmsAS5048AHighResolutiona}.}
    \label{fig:AStiming}
\end{figure}

\chapter{AS5048A Capacitance}
\label{app:AS5048AdatasheetCap}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Astable}
    \caption{AS5048A capacitance\cite{AmsAS5048AHighResolutiona}.}
    \label{fig:AScap}
\end{figure}


\end{document}